# 问题5.md — 第 5 轮对比审计(镜头:分页/查询参数/HTTP 语义)

- 日期:2026-08-31
- 轮次:5 / 20(16 领域全扫 + 分页/查询参数/HTTP 语义镜头)
- 基线版本:同前(pinned OpenAPI `690521b`、openai-python `b19c2161`、openai-node `eea2292a`、Codex 0.144.5、rmcp 3.1.4)
- 第 4 轮修复回归:16 域全部通过,无回归
- 上轮遗留闭环:beta Responses WS 套接字同步(5-04)

## 汇总表

| 编号 | 严重度 | 领域 | 问题 | 状态 |
|---|---|---|---|---|
| 5-01 | 中低 | A | `follow_up_from` 复制 `conversation`,生成 pin 明文禁止的 previous_response_id+conversation 组合(服务端 400) | 已修复 |
| 5-02 | 中低 | A/F | beta 全部 7 个操作无 `OpenAI-Beta: responses_multi_agent=v1` 头发送面(python/node 均暴露 `betas` 参数并发送) | 已修复 |
| 5-03 | 中低 | E | `create_call` 省略 `session` 时应发裸 `application/sdp` 请求(两基线一致切换),Rust 恒 multipart | 已修复 |
| 5-04 | 中低 | E | 遗留:beta Responses WS 未同步 4-18/4-19(close code/reason、失败退役、显式 Pong 冗余) | 已修复 |
| 5-05 | 中低 | K | usage 的 `sources`/`sizes`/`context_levels` 仍为 `Vec<String>`(D0178 家族不完整) | 已修复 |
| 5-06 | 中低 | O | Codex `terminate` 先锁 writer 后杀进程,故障路径三方环锁 → 子进程泄漏 + close() 伪成功 | 已修复 |
| 5-07 | 低 | C/D | 手翻通道(`next_after`/`next_page`)未落实 D0147 回退;空串 last_id 经 D0145 丢弃后静默重复首页(chat×2/containers/admin) | 已修复 |
| 5-08 | 低 | E | GA `ResponsesWebSocket::recv` 非 decode 错误不置 closed(三个 recv 循环三种姿态) | 已修复 |
| 5-09 | 低 | E | WS 连接目标允许空串,`?model=`/`?call_id=` 空值上线路(node 拒绝空目标) | 已修复 |
| 5-10 | 低 | A | `BetaRetrieveResponseStreamParams` 缺 `stream=true` 反序列化防线,且 retrieve_stream 查询编码零测试 | 已修复 |
| 5-11 | 低 | B | input_items `limit` 无下限校验无区间文档(与同形 sibling 分叉) | 已修复 |
| 5-12 | 低 | B | `BetaResponseItemList` 缺 `extra_fields()`、`BetaListInputItemsParams` 缺 `includes()`(GA/beta 分叉) | 已修复 |
| 5-13 | 低 | B/J | `previous_response_id`×`conversation` 互斥句未随参数文档迁移(pin/python/node 均有) | 已修复(文档) |
| 5-14 | 低 | B | GA/beta `list_input_item_pages` fail-closed 路径零测试(重复游标/空 last_id) | 已修复 |
| 5-15 | 低 | J | conversations items `limit` 1..=100 上限为散文发明值(D0154/D0174 第三例未同步);`InvalidListLimit` 字段名未随 actual 约定 | 已修复 |
| 5-16 | 低 | J | voices consents `limit` 连 0 都放行(与 D0154 保留的 >=1 下限反向分叉) | 已修复 |
| 5-17 | 低 | K | `AuditLogListParams.effective_at` 为任意键 map(pin/python/node 均恰 4 键 gt/gte/lt/lte) | 已修复 |
| 5-18 | 低 | K | admin 信封游标 getter 缺 D0147 末项回退(`next_after_with` 辅助)+ audit flatten 继承 4 个未定义键的文档标注 | 已修复 |
| 5-19 | 低 | O | Codex 出站写路径无超时(stdin 管道满时公共 API 永久挂起;request_timeout 只覆盖响应等待) | 已修复 |
| 5-20 | 低 | O | `initialized` 通知多发送 pin 未定义的 `params: {}` 键 | 已修复 |
| 5-21 | 低 | O | 出站帧超限归入 `InvalidConfiguration` 错误类别错位(与 4-24 平台侧分叉) | 已修复 |
| 5-22 | 低 | O | server→client 请求(pinned 10 个)响应面零测试;stderr tail 注释 Bytes/chars 一字之差 | 已修复 |
| 5-23 | 低 | N | webhook 重试/幂等投递语义零论述;已校验的 `webhook-id`(官方推荐幂等键)在结果中被丢弃 | 已修复 |
| 5-24 | 低 | N | `verify` API 未警告"必须传原始未重序列化字节"(node 文档有专节) | 已修复(文档) |
| 5-25 | 低 | L | 代理能力缺失:四处硬编码 `.no_proxy()` 且无决策落档(企业代理网络零逃生门) | 已修复 |
| 5-26 | 低 | L | 零派生客户端面:长任务无法单独放宽 600s 总预算(提供 `with_request_timeout` 最小面) | 已修复 |
| 5-27 | 低 | L/H | `connect_timeout`/`request_timeout`/上传 64MB/8GB/1h 约束的 rustdoc 缺失 | 已修复(文档) |
| 5-28 | 低 | I/P | VS `before` 游标零端到端测试;RMCP `tools/list` 分页遍历语义与 10MiB 信封量级零文档 | 已修复 |
| 5-29 | — | 多域 | 决策落档类:multipart 成功码 200+201、containers DELETE Accept、X-Stainless 头省略、无压缩协商、连接池/h2 姿态、admin 无 list_pages、admin JSON Content-Type 严格门、`safety_identifier.blocked` 分歧、EventQueueFull fail-stop、Raw respond 面、codex cwd 立场、httpx MIME 猜测/filename 兜底 | 已修复(决策条目 D0223) |
| 5-R1 | — | L | X-Stainless 平台遥测头不发送 | 已并入决策(隐私立场,5-29) |
| 5-R2 | — | M | python 平铺错误体顶层取字段 | 已拒绝(4-R2 重申) |

## 关键详情(摘要)

- **5-01**:responses.rs:5900-5914 `follow_up_from` 经 `follow_up` 置 `previous_response_id` 后原样拷贝 `conversation`(5913);pin `ResponseProperties.previous_response_id` 明文 "Cannot be used in conjunction with `conversation`."。修复:不再拷贝 conversation + rustdoc 字段清单修正 + 镜像测试。
- **5-02**:pin 7 个 beta 操作声明可选 header `openai-beta`(enum `["responses_multi_agent=v1"]`);python `betas` 参数→头、node 同;Rust 测试反而断言 `beta_header: None` 且无逐请求头逃生门。修复:7 个 beta 操作改走 `execute_*_with_static_header` 固定发送 `OpenAI-Beta: responses_multi_agent=v1`(D0087/chatkit 模式);更新两处测试断言。
- **5-03**:pin `POST /realtime/calls` requestBody 双 content-type;python `_multipart.py` 与 node `multipart-encoding.ts` 均在"body 仅含 sdp"时切裸体 `application/sdp`。修复:session 为 Omitted 时直接发 sdp 文本 + `Content-Type: application/sdp`;有 session 维持两部件 multipart;补 wire 测试。
- **5-04**:按 E 域 8 步方案同步 beta WS:Close 帧保留+last_close 字段+close_code()/close_reason();读错误/超限/Binary/Frame 退役置 closed;移除显式 Pong(移植 D0148 注释);三个镜像测试。
- **5-06**:client.rs:1171 terminate 在 1182 start_kill 之前锁 writer;写方无超时持锁。修复:先 take child 并 `start_kill()`(管道断裂释放 writer 锁)再收 writer;或 writer 锁加 shutdown_timeout 上限。补环锁回归测试(阻塞写 + 触发 terminate)。
- **5-07**:chat.rs:3542/3703 `next_after` 为 `has_more.then_some(last_id.as_str())`;修复为与 `pagination::next_cursor` 同规则(过滤空串→回退 data.last() id→None),`next_page` 调用侧接入;containers.rs:768、admin 信封(见 5-18)同步。
- **5-19**:client.rs:582 write 在 timeout 块外;修复:写阶段并入 request_timeout 预算(timeout 包 write+receiver)。
- **5-23**:`VerifiedWebhook` 增加 `webhook_id()`(verify 时已读取,存储 + 访问器);模块文档补投递语义段(72h 指数退避、3xx 视为失败、快速 2xx、重复投递、以 webhook-id 去重)。
- **5-25**:决策落档"与 node 对齐不读 env 代理"+ `ClientBuilder` 提供显式 `proxy(Option<reqwest::Proxy>)` 透传面(默认 None 维持 no_proxy;设置后启用);admin/x509/workload 三处 rustdoc 注明立场。
- **5-26**:`Client::with_request_timeout(Duration) -> Client`(Arc 派生克隆,覆盖 Inner 的 request_timeout);决策记录 typed-first 立场与该最小逃生门。

## 决策落档类(5-29,主代理)

1. multipart lane 成功码 200+201 vs JSON 精确 200(维持现状落档);2. containers DELETE 的 `Accept: *//*` vs application/json(EmptyOrJson 族立场);3. X-Stainless 头全集省略(隐私,read-timeout 提示一并放弃);4. 无 Accept-Encoding 压缩协商(带宽换 CPU);5. 连接池默认(90s idle/无上限)+ h2-by-default 姿态;6. admin 通道无 list_pages(仅手翻,配 next_after_with 辅助);7. admin JSON Content-Type 严格门(防误路由);8. `safety_identifier.blocked` 事件:pin 18 为准,python 多出该类型化事件、node 16,Unknown 兜底;9. Codex EventQueueFull fail-stop(消费者停顿拆连接,容量可配);10. server→client 请求响应保持 Raw respond_result/respond_error(泛型面);11. Codex spawn cwd=CODEX_HOME + env 白名单无 HOME(隔离立场);12. httpx 扩展名 MIME 猜测与 filename 兜底值三方差异(Rust 站 node 侧)。

## 结论

分页/HTTP 语义镜头:1 中低核心(follow_up_from 互斥组合)+ 5 中低(beta 头/裸 SDP/beta WS 同步/usage 枚举/codex 环锁)+ 22 低 + 12 决策落档;第 4 轮修复回归 16/16 通过,无回归项。

## 修复记录(2026-08-31 执行完毕)

- 29/29 项闭环:5-01~5-28 代码/文档修复(约 50 个新测试函数),5-29 十二项立场以 D0223 落档。决策条目 D0209–D0223(15 条)已追加 `spec/contracts/decisions.md`(账本累计 223 条)。
- 修复分组:组1 responses/beta(5-01/02/10/11/12/13/14)、组2 手翻回退 chat/containers(5-07)、组3 WS 三客户端统一+裸 SDP(5-03/04/08/09)、组4 limit 统一/VS 测试/上传文档(5-15/16/27/28)、组5 admin 收窄(5-05/17/18/25/07-admin)、组6 webhooks 幂等键(5-23/24)、组7 codex 环锁/写超时(5-06/19/20/21/22)、组8 client 代理面/超时派生(5-25/26/27)、组9 rmcp 文档(5-28)。
- 主代理收尾:组1 报告的 beta delete/SSE 静态头缺口——transport.rs 补 `execute_optional_json_with_static_header` 与 pub(crate) `send_with_static_header`,beta delete/create_stream/retrieve_stream 全部接线发送 `OpenAI-Beta: responses_multi_agent=v1`,三处测试断言更新(D0210 全 lane 覆盖)。
- 审计修正:5-05 的 pin 核实 sizes 含 `1792x1792`(非任务书笔误的 1792x1024),按 pin 建域并注明与生成侧 ImageSize 的域差异。
- 全部门禁通过:fmt、clippy(-D warnings 零错)、全量测试 **904 passed / 0 failed**、no-default-features、xtask check、cargo deny。
- 遗留:无新待定项;voices limit 构造期急切拒绝受链式调用签名约束(serde 边界强制已闭环,见 D0217)。
