# 问题11.md — 第 11 轮复扫(R1 镜头第二遍 + node/python 交叉验证)

- 日期:2026-09-01
- 轮次:11 / 20(R1 全量基线复扫;10 个领域零发现)
- 基线:同前 + schemars 1.2.2 源码、MCP 2026-07-28 规范、httpdate 1.0.3 源码
- 第 10 轮修复回归:全部通过

## 汇总表

| 编号 | 严重度 | 领域 | 问题 | 状态 |
|---|---|---|---|---|
| 11-01 | 高 | L | strict schema 归一化放行 schemars 根部 `$schema` 键直达 wire(draft2020-12 根 schema 恒含该键;生态证据表明服务端 strict 校验会 400;与 D0129 "never silently drops" 契约矛盾) | 已修复 |
| 11-02 | 中低 | A | `PromptReference.variables` 非 Nullable(pin anyOf[map,null];python/node 均可空;decode `variables: null` 回显会击穿 Response) | 已修复 |
| 11-03 | 中低 | F | beta create `validate()` 遗漏 beta 自身 `context_management` 的 EmptyContextManagement/CompactThreshold 校验(base.validate() 看不到被拆出的字段);`BetaCreateStreamingResponseRequest` 缺 `validate()` 入口 | 已修复 |
| 11-04 | 低 | H | `Files` 缺 `wait_for_processing` 轮询辅助(python/node 均有;终态含 `deleted`(Unknown 兜底);error 终态返回对象不抛错) | 已修复 |
| 11-05 | 低 | L | `validate_name` 64 上限对 Responses 工具名严于 pin(128);与 FunctionTool::new 不校验形成不对称 | 已修复 |
| 11-06 | 低 | K | admin 清单 SHA-256 溯源常量已陈旧(=pin 引入时哈希)且全仓零消费点,提供虚假溯源保证 | 已修复 |
| 11-07 | 低 | P | catalog.rs 丢弃清单含 rmcp 3.1.4 与 MCP 2026-07-28 均不存在的 `execution` 字段(2025-11-25 修订遗留;`mcp_tool()` 逃逸为假) | 已修复 |
| 11-08 | 低 | P | 门面 `rmcp` 别名只 gate on `rmcp` feature;`rmcp-server`/`rmcp-server-stdio`/`rmcp-auth` 组合下编译 crate 但别名不可达(7-24 同族) | 已修复 |
| 11-09 | 低 | C | `trace::tests::sse_stream_deltas_never_enter_tracing` 并行下 flaky(线程局部 subscriber 与 libtest 线程池竞态;单线程稳定通过) | 已修复 |
| 11-10 | — | 多域 | 落档类:L11-2(validate_name 决策随 11-05)、L11-3(bare `#` vs 官方递归示例张力)、K2(Certificate.active 并集)、F3(beta retrieve 非流式参数收窄)、G2(speech stream_format 无 builder)、J2(skill_id/data validate 缺口)、N2/N3(文档出处措辞、双份 realtime.call.incoming)、O1(RpcId u64 域窄已决策) | 已修复(决策条目 D0247-D0250) |
| 11-R1 | — | D/E/G/I/M | 全量复扫 | 零发现(附覆盖面) |
| 11-R2 | — | B/J/N | 全量复扫 | 零发现 |

## 关键详情

- **11-01(高)**:schemars 1.2.2 `schema_for!` = draft2020-12 生成器,`into_root_schema_for` 无条件在根插入 `"$schema": "https://json-schema.org/draft/2020-12/schema"` 且无移除 transform;`normalize` 黑名单不含 `$schema` → `StructuredOutput::new`/`TypedFunction::new`/`FunctionTool::for_type` 三个公开入口全部携带该键上线。python 不受影响因 pydantic 不产出该键(但产出 title——title 被接受);node zod openai target 剥离。修复:`normalize_strict_schema` 入口剥离根 `$schema`(对齐生态惯例),或加黑名单显式报错;补"生成 schema 不含 `$schema`"回归测试。
- **11-02(中低)**:D0034 修了同结构体的 `version` 但未覆盖 `variables`;`Response.prompt` 用该类型解码回显,`variables: null` 直接报错。修复:`Omittable<Nullable<BTreeMap>>` + `variables_null()`。
- **11-03(中低)**:beta 结构体把 `context_management` 从 base 拆出独立存放,`base.validate()` 永远看不到;GA 通道对同一 wire 字段执行两项检查(responses.rs:5803-5816)。现成的 `CreateResponseConstraintError::{EmptyContextManagement, CompactThreshold}` 可经 beta envelope 复用。streaming 类型补 `validate()`(镜像 GA 宏行为)。
- **11-04(低)**:终态 `{processed, error, deleted}`;error 返回 FileObject 不抛错(python/node 同);超时报错。补 `PollOptions::for_files`(5s/30min 对齐 python 默认)。

## 结论

R1 复扫收敛明显:10/16 领域零发现;1 高(structured `$schema` 泄漏——真正的 wire 级缺陷,前轮镜头盲区)+ 2 中低 + 6 低 + 落档类。第 10 轮修复回归全部在位。

## 修复记录(2026-09-01 执行完毕)

- 10/10 项闭环:11-01~11-09 代码修复(约 25 个新测试函数),11-10 落档类观察以 D0247-D0250 落档。决策条目 D0247–D0250(4 条)已追加 `spec/contracts/decisions.md`(账本累计 250 条)。
- 修复分组:组1 structured/responses(11-01 根 $schema 剥离/11-02 variables 三态/11-05 名称上限分径)、组2 beta/files/admin/rmcp/facade/trace(11-03/04/06/07/08/09)。
- 关键修复:11-01 是本轮唯一的 wire 级缺陷(schemars 根 `$schema` 键直达 strict 端点会 400)——R1 复扫镜头的价值证明。
- 全部门禁通过:fmt、clippy(-D warnings 零错)、全量测试 **1129 passed / 0 failed**、no-default-features、xtask check、cargo deny;SSE no-leak 测试重复 5 次零失败。
- 遗留:executor.rs 在 auth-only 特性组合下的既存 unused import 警告(非 all-features 触发)——D0250 记录,下轮顺手。
