# 问题1.md — 第 1 轮对比审计(全量基线扫描)

- 日期:2026-08-31
- 轮次:1 / 20(镜头:16 领域全量基线)
- 审计方式:16 个并行分析代理,逐领域对照 pinned OpenAPI、openai-python、openai-node 与官方文档
- 基线版本:
  - pinned OpenAPI:`spec/upstream/openapi-2026-08-29.json`(rev `690521b1753dce0c6d6b275f583d22537679cff9`,sha256 见 `spec/SOURCES.toml`)
  - openai-python:`b19c2161b1eac80fbf1f6f67a64a50af99c53356`
  - openai-node:`eea2292a4a523da9405161dde0a79ac5dc2ecb2a`(`manual-overrides.toml` 已引用 commit)
  - Codex:`spec/upstream/codex/0.144.5/codex_app_server_protocol.schemas.json`(sha256 pinned)
- 覆盖领域:A Responses请求 / B Responses响应 / C Responses流式 / D Chat / E Realtime / F Beta+legacy / G Media / H Files+Uploads+Batches / I FT+VS+Containers / J 杂项资源 / K Admin / L 共享核心 / M 传输层 / N Webhooks / O Codex / P RMCP+门面+examples
- 无新发现领域:H(Files/Uploads/Batches)、I(FT/VS/Containers)——与三方基线逐字段一致

## 汇总表

| 编号 | 严重度 | 领域 | 问题 | 状态 |
|---|---|---|---|---|
| 1-01 | 中高 | B | `FileSearchResult.attributes` 拒绝显式 null,整个 Response 解码失败 | 已修复 |
| 1-02 | 中 | B | `CompactedResponse.output` 无法解码 compact 返回的 user 角色消息 | 已修复 |
| 1-03 | 低 | B | `Response.store` 非官方 echo 字段且非 Nullable,null 回显会解码失败 | 已修复 |
| 1-04 | 低 | B | 7 处官方枚举域以裸 `String` 建模 | 已修复 |
| 1-05 | 低 | A | `NamespaceTool.tools` 元素域宽于 pin(应仅 function/custom) | 已修复 |
| 1-06 | 低 | A | `CodeInterpreterNetworkAllowlist` 缺官方可选字段 `domain_secrets` | 已修复 |
| 1-07 | 低 | C | SSE 终止事件表含 pin 外的 `response.cancelled`,与 `is_terminal()` 不一致 | 已修复 |
| 1-08 | 低 | C | retrieve 侧 `include`/`order` 查询参数弱类型 | 已修复 |
| 1-09 | 低 | D | 已废弃 `functions[]` 复用带 `strict` 的函数定义,可发出 pin 外字段 | 已修复 |
| 1-10 | 中 | E | Realtime 无 `intent=transcription` 连接入口,GA 转写会话不可达 | 已修复 |
| 1-11 | 低 | E | Realtime WS 不支持 `call_id` 附着既有通话 | 已修复 |
| 1-12 | 低 | E | legacy realtime 对两个端点发送 `OpenAI-Beta: assistants=v2` | 已修复 |
| 1-13 | 信息 | E | `RealtimeAudioTranscription.delay` 复用 `RealtimeReasoningEffort` 类型名 | 已修复 |
| 1-14 | 中 | G | multipart 元数据 JSON null 编码为字面量 `"null"` 文本 | 已修复 |
| 1-15 | 低 | J | `ConversationFunctionCall` 缺 `caller`/`namespace`/`created_by` 类型化字段 | 已修复 |
| 1-16 | 低 | K | `InviteRole` 含 pin 之外的第 3 个变体 `member` | 已修复 |
| 1-17 | 低 | K | `DataRetentionType` org/project 共享 6 值域,org 侧为超集 | 已修复 |
| 1-18 | 高 | L | `normalize_strict_schema` 不解开带兄弟键的 `$ref`,产出 strict 校验拒绝的 schema | 已修复 |
| 1-19 | 中 | L | `normalize_object` 无条件覆写已存在的 `additionalProperties`,map 字段静默失效 | 已修复 |
| 1-20 | 低 | L | `ModelId` 常量域与 pin 不符(含不存在的 `gpt-5.6`,漏 `gpt-5.6-cyber`) | 已修复 |
| 1-21 | 低 | M | 默认服务器重试延迟上限 60s,低于 openai-python 的 120s | 已修复 |
| 1-22 | 低 | M | `Retry-After: 0` 零延迟立即重试(python 落入本地退避) | 已修复 |
| 1-23 | 低 | M | Realtime 幂等 `client_secrets` 操作被标为 `RetryClass::Never` | 已修复 |
| 1-24 | 中 | N | realtime.rs 重复定义的 `realtime.call.incoming` 将 `object` 设为必填,拒绝官方示例 | 已修复 |
| 1-25 | 中 | N | realtime.rs 重复类型 Debug 原样打印 SIP 头(可含凭据),与 webhooks.rs 脱敏红线不一致 | 已修复 |
| 1-26 | 高 | O | Codex `UserInput` 含 schema 不存在的 `audio`/`localAudio` 变体 | 已修复 |
| 1-27 | 中高 | O | Codex `account/usage/read` 的 `threadId` 参数与 `threadUsage` 字段均为 schema 所无 | 已修复 |
| 1-28 | 中 | O | Codex `InitializeCapabilities` 缺 `mcpServerOpenaiFormElicitation`,发明 `extensions` | 已修复 |
| 1-29 | 中低 | P | 门面 crate 未透传 `RetrieveResponseParams` 等公开类型与 `sse` 模块 | 已修复 |
| 1-30 | 低 | P | `examples/responses.rs` 续轮未重发 tools,偏离官方示例 | 已修复 |
| 1-R1 | — | A | `item_reference` 无 typed 分支(仅信息性) | 已拒绝 |
| 1-R2 | — | F | `BetaAgentMessageContent` 请求侧超集 | 已拒绝(D0103 已决策) |
| 1-R3 | — | O | `RpcId::Number(u64)` 不接受负整数 | 已拒绝 |
| 1-D1 | — | F | Beta agent item 资源端 `id` 官方必填(需请求/资源拆分重构) | 待定 |
| 1-D2 | — | G | multipart 空串值 Python 丢弃而 Rust 照发 | 待定 |
| 1-D3 | — | M | connect_timeout 默认 10s vs python 5s | 待定 |

## 问题详情与证据

### 1-01 `FileSearchResult.attributes` 拒绝显式 null【中高】
- 位置:`crates/openai-rs-types/src/responses.rs:9223`
- 证据:pin `FileSearchToolCall.results[].attributes` → `$ref VectorStoreFileAttributes`,该 schema 为 `anyOf [{16 键 map}, null]`;openai-python `response_file_search_tool_call.py:12` 为 `Optional[Dict[...]] = None`。
- 分析:`Omittable<T>` 遇 `null` 进入 `T::deserialize` 失败,`results` 为严格 `Vec`,失败向上冒泡使 `FileSearchCall`→`Response` 整体解码失败。触发条件:`include=["file_search_call.results"]` 且服务端发 `"attributes": null`。
- 修复:改为 `Omittable<Nullable<BTreeMap<String, FileSearchAttributeValue>>>`,getter 返回 `Option`,补 null/缺失/有值三态测试。

### 1-02 `CompactedResponse.output` 无法解码 user 角色消息【中】
- 位置:`crates/openai-rs-types/src/responses.rs:7261`;beta 同构 `beta_responses.rs:2858`
- 证据:pin `CompactResource.output.items` → `$ref ItemField`,其 message 分支 `role` 为任意 `MessageRole`;pin 官方示例 `output[0]` 即 `{"type":"message","role":"user",...}`;python `compacted_response.py` 文档字符串明言含 user 消息与 compaction item。
- 分析:Rust `ResponseOutputItem::Message` 严格解码为 assistant-only 的 `OutputMessage`,role:"user" 直接报错,compact 端点 200 体含 user 消息即整体解码失败。
- 修复:`output` 改用 pin `ItemField` 等价联合(输入侧 `ResponseInputItem` 的 `StoredMessage` 接受 user/system/developer),或为 compact 单独定义 message 分支;beta 侧同步。

### 1-03 `Response.store` 非官方 echo 字段且非 Nullable【低】
- 位置:`crates/openai-rs-types/src/responses.rs:6600`
- 证据:pin 的 `Response`/`ResponseProperties`/`ModelResponseProperties`/`CreateModelResponseProperties` 属性清单均无 `store`;openai-python `response.py` 与 openai-node 响应侧均无;beta 侧 `BetaResponse` 亦无。请求侧 pin `store` 为 `anyOf [bool, null]`,而 Rust 响应侧是 `Omittable<bool>`。
- 修复:改为 `Omittable<Nullable<bool>>` 三态(保留便捷性,消除 null 回显解码失败;字段是否移入 ExtraFields 由修复代理按最小破坏权衡并在决策条目说明)。

### 1-04 7 处官方枚举域以裸 `String` 建模【低】
- 位置/证据(pin 枚举 + python `Literal` 双边):
  - `ProgramOutputItem.status`(responses.rs:9163)← `ProgramOutputStatus` = completed|incomplete
  - `ApplyPatchCallInput.status`(12374)、`ApplyPatchCall.status`(12457)← `ApplyPatchCallStatus(Param)` = in_progress|completed
  - `ApplyPatchCallOutput(Input).status`(12487/12577)← `ApplyPatchCallOutputStatus(Param)` = completed|failed
  - `ToolSearchCall/Output/Input.execution`(10666/10678/10574/10709)← `ToolSearchExecutionType` = server|client
- 修复:按 D0106 模式建 `open_string_enum`(如 `ProgramOutputStatus`、`ApplyPatchCallStatus`、`ApplyPatchCallOutputStatus`、`ToolSearchExecution`),未知值 `Unknown` 保持无损。

### 1-05 `NamespaceTool.tools` 元素域宽于 pin【低】
- 位置:`crates/openai-rs-types/src/responses.rs:14972`
- 证据:pin `NamespaceToolParam.tools.items` 为 `oneOf [FunctionToolParam, CustomToolParam]`;python `namespace_tool_param.py:42,54` `Tool = Union[ToolFunction, CustomToolParam]`。
- 修复:收窄为 function/custom 专用枚举,或在 `validate_response_tool` 的 namespace 分支校验元素 tag ∈ {function, custom}。

### 1-06 `CodeInterpreterNetworkAllowlist` 缺 `domain_secrets`【低】
- 位置:`crates/openai-rs-types/src/responses.rs:14493-14499`
- 证据:pin `ContainerNetworkPolicyAllowlistParam` 含可选 `domain_secrets`(minItems 1,元素 `{domain,name,value}`,value ≤10485760),被 Responses 的 code-interpreter `network_policy` 引用;python 已类型化。
- 修复:增加 `domain_secrets` 字段(`Omittable<Vec<...>>` + builder),复用 D0076 的 domain-secret 结构与长度校验。

### 1-07 SSE 终止事件表含 pin 外的 `response.cancelled`【低】
- 位置:`crates/openai-rs-client/src/sse.rs:647`;不一致点 `crates/openai-rs-types/src/responses.rs:8367-8372`(`is_terminal()` 只认 Completed/Failed/Incomplete/Error)
- 证据:pin `ResponseStreamEvent` 58 个判别值无 `response.cancelled`(仅存在于 webhook);python/node 无对应流事件。
- 修复:从 `responses()` 策略移除 `response.cancelled`,使 SSE 策略与 `is_terminal()` 口径一致;补策略表测试。

### 1-08 retrieve 侧 `include`/`order` 查询参数弱类型【低】
- 位置:`crates/openai-rs-client/src/responses.rs:35-52`;对照 types 侧 `ListResponseInputItemsParams.include: Vec<ResponseIncludable>`(responses.rs:7295-7304)
- 证据:pin 两个 GET 的 `include` 均为 `array of IncludeEnum`;python `response_retrieve_params.py:14` 为 `List[ResponseIncludable]`;`order` pin 为 enum asc|desc。
- 修复:`RetrieveResponseParams.include`/`RetrieveResponseStreamParams.include` 改 `Vec<ResponseIncludable>`;`order` 收窄为开放枚举。

### 1-09 chat `functions[]` 复用带 `strict` 的函数定义【低】
- 位置:`crates/openai-rs-types/src/chat.rs:1229-1242`(strict)、`2316-2317`(functions 字段)
- 证据:pin `CreateChatCompletionRequest.functions.items -> ChatCompletionFunctions` 无 `strict`(仅 `tools[].function -> FunctionObject` 有);python `completion_create_params.py:388-404` 的 `Function` TypedDict 无 strict;node 同。
- 修复:为 `functions[]` 使用不含 `strict` 的独立 DTO,与 `tools[].function` 分离。

### 1-10 Realtime 无 `intent=transcription` 连接入口【中】
- 位置:`crates/openai-rs-client/src/realtime.rs:532-554`(`realtime_websocket_url` 只追加 model)、`57-68`(connect 只接受 model)
- 证据:openai-node `src/realtime/internal-base.ts`(246-304)连接目标为 `model` | `intent:'transcription'` | `callID` 三选一,转写分支 `url.searchParams.set('intent','transcription')` 且禁止同时给 model;官方/社区确认转写会话 URL 为 `wss://api.openai.com/v1/realtime?intent=transcription`(带 model 会被拒)。类型层已建模 `RealtimeTranscriptionSessionCreateRequest` 但客户端不可达。
- 修复:增加连接目标枚举(model | transcription intent | call_id),URL 分别落 `?model=...`/`?intent=transcription`/`?call_id=...`,补 URL 派生测试。

### 1-11 Realtime WS 不支持 `call_id`【低】
- 位置:同 1-10
- 证据:openai-python `realtime.py:99-137` `connect(*, call_id, model, ...)`;node `callID` 分支 `url.searchParams.set('call_id', ...)`。
- 修复:并入 1-10 的连接目标枚举。

### 1-12 legacy realtime 发送 `assistants=v2` beta 头【低】
- 位置:`crates/openai-rs-client/src/legacy_realtime.rs:24-25`、`61-62`、`80-82`
- 证据:pin 两个 operation `parameters` 均为空;`assistants=v2` 是 Assistants 族 beta 头(D0087 为 Vector Store 专用),与 Realtime 无关。
- 修复:去掉该头;补请求头断言测试。

### 1-13 `RealtimeAudioTranscription.delay` 类型命名错位【信息】
- 位置:`crates/openai-rs-types/src/realtime.rs:628`
- 证据:pin `AudioTranscription.delay` enum = minimal|low|medium|high|xhigh,值域恰与 `RealtimeReasoningEffort` 全等,wire 一致但语义误导。
- 修复:引入 `RealtimeTranscriptionDelay` 独立枚举(同值域),原处改用新类型。

### 1-14 multipart 元数据 JSON null 编码为字面量 `"null"`【中】
- 位置:`crates/openai-rs-client/src/media.rs:778`(`Value::Null => form.text(name, "null")`)
- 证据:python `_base_client.py:622-647` + `_qs.py:118-129`:显式 None 的 multipart 字段整体丢弃;node `uploads.ts:458-463`:显式 null 抛错除非用户主动传字符串 "null"。pin 对 multipart 体内 null 承载未定义。可触达字段:`TranscriptionRequestMetadata.chunking_strategy`(Omittable<Nullable<..>>)、`ImageEditMultipartMetadata` 的 9 个字段。
- 修复:`Value::Null` 时跳过该字段(对齐 python 自动行为);补 loopback multipart 测试锁定行为。

### 1-15 `ConversationFunctionCall` 缺 `caller`/`namespace`/`created_by`【低】
- 位置:`crates/openai-rs-types/src/conversations.rs:1115-1172`
- 证据:pin `FunctionToolCall` 含 `caller`(anyOf ToolCallCaller|null)、`namespace`;`FunctionToolCallResource` 另有 `created_by`。Responses 侧同 schema 的 `responses::FunctionCall` 已按 D0030 类型化这三个字段;conversations.rs 相邻分支 `FunctionCallOutput` 也已类型化,构成文件内不一致。
- 修复:增加 `Omittable<String>` 的 `namespace`/`created_by` 与 `Omittable<Nullable<ToolCallCaller>>` 的 `caller`,复用 responses 侧形状与测试。

### 1-16 `InviteRole` 含 pin 之外变体 `member`【低】
- 位置:`crates/openai-rs-types/src/admin.rs:1793-1800`
- 证据:pin `InviteRequest.role` enum=["reader","owner"];python `invite_create_params.py:15`、node `invites.ts:128,180` 同(嵌套 projects[] 的 member 已由 `InviteProjectRole` 正确承载)。
- 修复:删除 `Member` 变体(未知值已有 `Unknown` 兜底);修正受影响测试。

### 1-17 `DataRetentionType` org/project 共享超集【低】
- 位置:`crates/openai-rs-types/src/admin.rs:1309-1316`、`1340-1344`
- 证据:pin org 侧(`UpdateOrganizationDataRetentionBody`/`OrganizationDataRetention.type`)为 4 值,project 侧为 6 值(另含 `organization_default`、`none`);node 保持拆分。
- 修复:org 层请求体引入 4 值枚举(或 `validate()` 拒绝 org 侧超集值),资源侧可保留开放超集。

### 1-18 `normalize_strict_schema` 不解带兄弟键的 `$ref`【高】
- 位置:`crates/openai-rs-types/src/structured.rs:469-476`、`543-551`
- 证据:schemars 1.2.2(Cargo.lock 锁定)对"字段类型为另一个 derive(JsonSchema) 自定义类型"的字段经 `subschema_for` 返回裸 `{"$ref":"#/$defs/X"}`,字段 doc 注释等经 `Schema::insert` 注入同一对象得到 `$ref`+兄弟键;`Option<Inner>` 场景 anyOf 首分支同样带兄弟键。官方 python `_pydantic.py:93-117` 显式解引用(注释原文 "we can't use `$ref`s if there are also other properties defined … so we unravel the ref");官方文档所有 `$ref` 示例均为唯一键。
- 影响:泛型参数嵌套类型字段带 doc 注释(常见写法)时,strict 请求 400。
- 修复:模仿 python 的 `resolve_ref` + 兄弟键检测:本地 `#/$defs`/`#/definitions` ref 伴随兄弟键时按 JSON Pointer 解析内联(兄弟键优先)再重入 normalize;无法解析时报 `UnsupportedKeyword` 类错误而非静默发送;补带 doc 注释嵌套类型的端到端测试。

### 1-19 `normalize_object` 覆写已存在的 `additionalProperties`【中】
- 位置:`crates/openai-rs-types/src/structured.rs:530`
- 证据:schemars 对 `HashMap<String,T>` 生成 `{"type":"object","additionalProperties":{"type":"string"}}`;normalize 后被覆写为 `false`,字典字段静默变为只能输出 `{}`,与模块头"never silently drops a schema keyword"契约矛盾。python 仅在缺失时补 false(`_pydantic.py:50-51`);node 把 record 直接判不支持并抛错(`zod-v3-strict-schema.ts:67-69`)。
- 修复:已存在 `additionalProperties` 且值非 `false` 时返回 `StructuredError::UnsupportedKeyword`,仅缺失时补 `false`;补 map 字段测试(拒绝 + 错误信息含路径)。

### 1-20 `ModelId` 常量域与 pin 不符【低】
- 位置:`crates/openai-rs-types/src/scalar.rs:287-297`;连带 `crates/openai-rs/examples/structured_output.rs:26`
- 证据:pin 模型枚举含 `gpt-5.6-sol/terra/luna` 与 `gpt-5.6-cyber`,无裸 `gpt-5.6` 值(spec 仅描述性文字提及);node `shared.ts` 同账。Rust 的 `GPT_5_6 = "gpt-5.6"` 及"official alias"注释无基线支撑,且缺 `GPT_5_6_CYBER`。
- 修复:补 `gpt-5.6-cyber` 常量,移除 `gpt-5.6` 常量(或降格并改写注释去掉"official alias"表述),示例换用 spec 内模型名。

### 1-21 默认服务器重试延迟上限 60s vs python 120s【低】
- 位置:`crates/openai-rs-client/src/retry.rs:19`、`transport.rs:388-390`
- 证据:python `_constants.py:13` `MAX_RETRY_AFTER_DELAY = 2*60`,超限回退本地退避仍重试;Rust 超限直接放弃重试。
- 修复:`openai_compatible()` 默认 `max_server_delay` 提到 120s;补边界测试(60-120s 区间遵守服务器指示)。

### 1-22 `Retry-After: 0` 零延迟立即重试【低】
- 位置:`crates/openai-rs-client/src/transport.rs:756-765`
- 证据:python `_base_client.py:800` 仅 `0 < retry_after` 时采用服务器值,0 落入本地退避(≥0.5s)。
- 修复:`seconds <= 0.0` 按 `ServerDelay::Absent` 处理;补 0 值测试。

### 1-23 Realtime 幂等 `client_secrets` 不重试【低】
- 位置:`crates/openai-rs-client/src/realtime.rs:619`(宏硬编码 `RetryClass::Never`);`legacy_realtime.rs:104,124`
- 证据:python/node 重试循环对所有请求无差别生效;`client_secrets` 令牌签发幂等,瞬时 429/5xx 下可用性低于官方。呼叫控制动作(accept/reject/hangup/refer)保持 Never 是保守合理,保留。
- 修复:`CreateClientSecret`/`CreateTranslationClientSecret` 单独标为 `Replayable`,其余维持。

### 1-24 realtime.rs 重复 webhook 类型 `object` 必填【中】
- 位置:`crates/openai-rs-types/src/realtime.rs:3106-3118`
- 证据:pin `WebhookRealtimeCallIncoming.required=["created_at","id","data","type"]`(object 不必填),官方示例不含 `object`;python `realtime_call_incoming_webhook_event.py:53` 为 `Optional[Literal["event"]]`;node `object?: 'event'`。webhooks.rs:499-506 对同一事件已正确用 `optional_object`;realtime.rs:5145-5157 测试 fixture 人为补 object 才通过。
- 修复:`object` 改 `Omittable<...>`(`#[serde(default, skip_serializing_if)]`),或删除重复类型复用 webhooks.rs 定义;修正 fixture。

### 1-25 realtime.rs SIP Debug 未脱敏【中】
- 位置:`crates/openai-rs-types/src/realtime.rs:3082-3097`(`RealtimeSipHeader` derive Debug)、`3107`
- 证据:SIP INVITE 可携带 `Authorization`/`Proxy-Authorization`;webhooks.rs:218-225 的 `SipHeader` 手写 Debug 不输出 name/value 并有 `payload_debug_does_not_leak_sip_headers` 测试;realtime 路径 `{:#?}` 完整输出。
- 修复:为 `RealtimeSipHeader`/`RealtimeCallIncomingData`/`WebhookRealtimeCallIncoming` 实现 redacted Debug;补泄露断言测试。

### 1-26 Codex `UserInput` 虚构 `audio`/`localAudio` 变体【高】
- 位置:`crates/openai-rs-codex/src/protocol.rs:350-355`;固化测试 616-621
- 证据:pinned schema `#/definitions/v2/UserInput/oneOf` 仅 5 变体(text、image、localImage、skill、mention);全 schema grep 无 `{"type":"audio"}`/`localAudio` 输入变体。
- 影响:发送即产生 0.144.5 服务端无法反序列化的 params。
- 修复:删除两个变体及固化测试;保留其余 5 变体。

### 1-27 Codex `account/usage/read` 虚构参数与字段【中高】
- 位置:`crates/openai-rs-codex/src/app_server/client.rs:450-459`、`protocol.rs:207-212`、`241`
- 证据:pinned schema 该方法 `params = {"type":"null"}`(只允许 null);`GetAccountTokenUsageResponse` 仅 `summary`/`dailyUsageBuckets`,无 threadUsage。
- 修复:移除 `AccountUsageParams` 与 thread_id 分支,固定 `request_without_params`;删除 `thread_usage` 字段;修正受影响调用与测试。

### 1-28 Codex `InitializeCapabilities` 字段不符【中】
- 位置:`crates/openai-rs-codex/src/protocol.rs:36-47`
- 证据:pinned schema 属性恰为 4 个:`experimentalApi`、`mcpServerOpenaiFormElicitation`、`optOutNotificationMethods`、`requestAttestation`;无 `extensions`。
- 修复:补齐 4 个真实能力字段(可空/Omittable 语义按 schema),删除 `extensions`(未来能力由 flatten extra 无损保留);更新握手测试。

### 1-29 门面 crate 缺 re-export【中低】
- 位置:`crates/openai-rs/src/lib.rs:18-39`
- 证据:`RetrieveResponseParams`/`RetrieveResponseStreamParams`(retrieve_with/retrieve_stream 按值收参,流恢复唯一入口)、`BodyPreview`、`RateLimitMetadata`、`pub mod sse` 在 client crate 公开但门面缺失,用户必须直接依赖 `openai-rs-client` 才能调用。
- 修复:client gate 下补 re-export 上述类型与 `sse` 模块(admin/content-provenance 的次要类型酌情);补编译测试。

### 1-30 `examples/responses.rs` 续轮未重发 tools【低】
- 位置:`crates/openai-rs/examples/responses.rs:41-49`
- 证据:官方 function-calling 指南所有 `previous_response_id` 续轮示例均同时重发 tools;本仓库 `CreateResponseRequest::follow_up` 文档自证 previous_response_id 不携带 tools。
- 修复:示例改用 `follow_up_from(&request, &response, ...)` 或 `.with_tool(tool)`,修正注释。

## 已拒绝项(附理由)

- **1-R1 `item_reference` 无 typed 分支**(A-3):序列化仅发 `{id}`(spec 合法),显式形式解码落 `Unknown` 无损保留——符合文件头部声明的未知 tag 设计,不构成 wire 错误。
- **1-R2 `BetaAgentMessageContent` 请求侧超集**(F-2):D0103 已明确决定"在共享联合上命名全部官方资源分支",请求 Param fixture 不作为收窄依据;维持现状。
- **1-R3 `RpcId::Number(u64)`**(O-4):真实 Codex 服务端生成非负 id,负 id 风险纯理论;保留 u64。

## 待定项(带入后续轮次)

- **1-D1** Beta agent 三类 item 资源端 `id` 官方必填(F-1):需请求/资源双形态拆分(参照 D0102/D0103 模式),影响面较大,下一轮评估。
- **1-D2** multipart 空串值是否对齐 python 丢弃语义(G-2):空 prompt 本就非法,收益低、行为变更风险高,待官方文档明确 multipart null/空串承载后再决策。
- **1-D3** connect_timeout 默认 10s vs python 5s(M-4):无 wire 影响,属默认值取舍,待定。

## 结论

16 领域中 H、I 两域零发现;A/B/C/D/G/J/K/L/M/N/O/P 共 30 项确认修复(2 高、6 中、22 低/信息级)。修复按文件所有权分组并行执行;每项修复遵循 CONTRIBUTING「Wire-contract changes」五步并在 `spec/contracts/decisions.md` 追加条目。

## 修复记录(2026-08-31 执行完毕)

- 30/30 项已修复,新增 37 个测试函数(多数函数内含多场景断言);决策条目 D0113–D0135 已追加至 `spec/contracts/decisions.md`。
- 修复分组:组1 Responses(1-01~1-08)、组2 structured/scalar(1-18~1-20)、组3 chat/conversations(1-09/1-15)、组4 realtime(1-10~1-13/1-23~1-25)、组5 media(1-14)、组6 admin(1-16/1-17)、组7 retry/transport(1-21/1-22)、组8 codex(1-26~1-28)、组9 门面/示例(1-29/1-30)。
- 交叉影响处理:门面测试 `facade_reexports.rs` 适配 1-08 类型收窄(改用 `ResponseIncludable` 命名变体);`multipart.rs` 的本地重试延迟副本与 1-21/1-22 新语义同步(非正值/超限回退本地退避)。
- 附带工程修复(非问题清单项):HEAD 基线 `openai-rs-types` 本带 66 条 clippy 违例(门禁在本轮前已破损)——47 条机械违例(needless_borrows/clone_on_copy/manual_ascii_range)已修复,4 处 large_enum_variant(ConversationItem/ResponseInputItem/ResponsesServerEvent/tagged_union 宏)以带理由的定点 allow 处理(Box 化属独立破坏性 API 重构,未混入本轮契约变更)。
- 全部门禁通过:`cargo fmt --check`、`cargo clippy --workspace --all-targets --all-features -D warnings`、`cargo test --workspace --all-features`(全绿)、`-p openai-rs-sdk --no-default-features`、`xtask check`、`cargo deny`。
- 遗留:待定项 1-D1/1-D2/1-D3 带入后续轮次;裸字符串 `"gpt-5.6"` 仍散布于多处测试 fixture/示例(开放 newtype 合法,建议后续轮统一替换);large_enum_variant 的 Box 化重构建议独立立项。
