# 问题9.md — 第 9 轮回归验证(D0113–D0245 全账本 + 门禁 + 漂移)

- 日期:2026-08-31
- 轮次:9 / 20(回归验证轮,无新修复)
- 结论:**全通过,零缺陷,零漂移,零 flake**。本轮无需代码改动。

## 回归结果

### 1. 决策账本 Tests 字段全量核对(2 个代理独立验证)

- D0113–D0245 共 133 条,Tests 字段反引号引用共 320 个(含 1 处花束写法展开)。
- **315 个为可运行测试**(`cargo test -- --list` 全 6 package 权威匹配;含 5 处双模块同名,各自存在),**5 个为非测试引用**(命令、shell 断言守卫、文件路径,逐一实证)。
- **不存在清单:空。**

### 2. 关键决策实测(24 条决策 / 82 次测试执行)

覆盖各轮代表:枚举收窄(D0115/D0189)、三态(D0114)、beta 头(D0123/D0210)、keepalive(D0171/D0239)、HMAC(D0161/D0235/D0205)、admin 重试(D0176/D0201)、错误通道(D0191/D0195/D0206)、传输(D0118/D0144/D0145)、codex(D0132/D0220/D0236)、rmcp(D0160/D0185/D0207/D0243)——**全部通过,0 失败**。

### 3. Overrides 关系链

范围内 15 条非 none Overrides(D0167→D0045 … D0240→D0030)全部目标存在、方向正确("后改前",无前向/自引用)。历史段(D0002–D0012 等)12 处字段缺失属模板未回填的历史形态,非断链。

### 4. 决策-代码一致性抽查(10 条:重点 5 + 随机 5)

D0114/D0210/D0171/D0131/D0195(重点)与 D0135/D0163/D0166/D0191/D0234(随机,种子 20260831)全部与当前代码一致。

### 5. 门禁全量(13 条命令,全部通过)

fmt / clippy(all-features、默认特性、最小特性)/ 全量测试 / no-default-features / workspace check / examples check / fuzz manifest check / MSRV 1.88.0 check ×2 / xtask check / cargo deny——**全部通过,零警告**。

### 6. 漂移扫描(六类历史标记,全部零残留)

裸 "gpt-5.6" 模型 id 用法 / GPT_5_6 常量 / BetaPromptCachedInputMessage 的 id_null+status_null / 已收窄构造面的 ResponseItemStatus / open_string_enum 的 Borrow / legacy_realtime 的 assistants=v2——全部零残留(media.rs:2213 的命中为第 8 轮新增测试的任意元数据关键字,非模型 id 用法)。spec 五投影与 codegen 输出零 diff。

### 7. 全量测试与 flake

`cargo test --workspace --all-features --locked`:**1101 通过 / 0 失败 / 2 ignored**。5 个时机敏感测试(keepalive 超时/重锚、HMAC 大候选量、重试退避、握手超时)各重复 3 次,**15/15 零 flake**。

### 8. 非阻断性观察(1 项)

D0236 的 "the eleven direct tests cited in the round record" 未显式点名出处(按计数恰好解析为 11 个现存且通过的测试)——纯文档措辞,下轮顺手可补。
