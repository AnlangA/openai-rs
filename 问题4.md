# 问题4.md — 第 4 轮对比审计(镜头:流式与错误通道)

- 日期:2026-08-31
- 轮次:4 / 20(16 领域全扫 + 流式/SSE/WS/错误通道镜头)
- 基线版本:同前(pinned OpenAPI `690521b`、openai-python `b19c2161`、openai-node `eea2292a`、Codex 0.144.5、rmcp 3.1.4)
- 第 3 轮修复回归:16 域通过;发现 1 处不完整(3-06 仍有 5 处宽域构造面,列 4-01)与 1 处未同步 beta(4-08)

## 汇总表

| 编号 | 严重度 | 领域 | 问题 | 状态 |
|---|---|---|---|---|
| 4-01 | 低(回归) | A/F | 3-06 不完整:5 处构造面仍 8 值 status(Computer/LocalShell/ToolSearch/Reasoning setter/new) | 已修复 |
| 4-02 | 中低 | B | 工具调用 output item 失败面零读访问器(status/id/result 等,python/node 均公开) | 已修复 |
| 4-03 | 低 | B | `output_parsed` 路由 Incomplete 不路由 Failed(错误信息丢失) | 已修复 |
| 4-04 | 低 | B/F | beta 响应与 WS 错误读访问器与 GA 分叉(BetaResponse.error 等;含 ExtraFields 缺失) | 已修复 |
| 4-05 | 低 | B | `ResponseAccumulatorError::Stream` 丢 param/sequence_number,null code 呈空串 | 已修复 |
| 4-06 | 中低 | C/F | legacy `/completions` 流缺 data 级 error 检查,错误降级为 Decode | 已修复 |
| 4-07 | 中低 | C | 5 个 media SSE 端点不接 `sse_limits` 配置(硬编码默认) | 已修复 |
| 4-08 | 低 | C/G | chat/completions/media 的 finish() 循环 Err 后不 return(违背 fail-stop) | 已修复 |
| 4-09 | 低 | C/D/L | `StreamError` Display/doc 固定 "Responses" 却被 chat/completions/media 复用 | 已修复 |
| 4-10 | 低 | C | response_stream 两循环 typed Error 处理不对称(防御性,不可达) | 已修复 |
| 4-11 | 低 | G | media 平铺 error 帧(无 event 行,`type:"error"`)被当 Unknown 事件,错误可完全丢失 | 已修复 |
| 4-12 | 低 | G | `error` 键 falsy 非空值({}/""/false)误判为 in-band 错误(python/node 按 truthiness) | 已修复 |
| 4-13 | 低 | G | media SSE 错误通道零测试覆盖 | 已修复 |
| 4-14 | 低 | F | `StreamErrorEvent` 缺 `param()` 访问器 | 已修复 |
| 4-15 | 低 | F | beta WS `type:"error"` 只 Ok 交付不进错误通道(两基线均抛错/emit)——最低限度补 is_error()+文档 | 已修复 |
| 4-16 | 低 | F | `BetaPromptCachedInputMessage::status` 仍 8 值(3-06 未同步 beta 侧) | 已修复 |
| 4-17 | 中低 | E | WS 握手失败(401/403/429)错误体整体丢弃,无 BodyPreview/信封字段 | 已修复 |
| 4-18 | 中低 | E | Close 帧 code/reason 被丢弃,异常关闭与干净 EOF 不可区分 | 已修复 |
| 4-19 | 低 | E | 非 keepalive 错误路径不置 closed,与注释承诺不符 | 已修复 |
| 4-20 | 低 | E/M | Realtime WS 握手无初始重连选项,与 Responses WS 不对称且无决策 | 已修复(决策+文档) |
| 4-21 | 低/中低 | H/I | Batch 轮询无 `for_batches()` 预设(默认 1s/10min 对 24h 窗口结构性不可用);poll 入口无预设提示;poll.rs 模块文档过时 | 已修复 |
| 4-22 | 低 | I | `InvalidMaxResults.maximum` 字段承载被拒值(应为 actual) | 已修复 |
| 4-23 | 中低 | J | EvalRun/RunGrader 失败面字段解码完整但零读取通道 | 已修复 |
| 4-24 | 低 | J | voices 10MiB 超限两阶段走两个错误类型(发送期复用语义错位的 InvalidConfiguration) | 已修复 |
| 4-25 | 低 | K/M | admin 错误体读取与平台分叉(超限→BodyTooLarge 丢信封;读中断→Transport 丢 status/request_id) | 已修复 |
| 4-26 | 中低(文档) | K | 一次性 `value` 铸造类 POST(admin key×3)× 默认自动重试的交互无 rustdoc/决策落档 | 已修复(文档+决策) |
| 4-27 | 低 | L/M | `StreamError` 丢弃官方必填 `type` 字段(两级 SDK 均暴露) | 已修复 |
| 4-28 | 低 | L | 错误信封单字段类型不符连坐丢失全部字段(python 逐字段宽松) | 已修复 |
| 4-29 | 低 | L | 宽松降级链/fallback 文案/畸形体零测试;Send/Sync 无断言 | 已修复 |
| 4-30 | 低 | L/M | 错误对象不保留 `Retry-After`/`x-should-retry`/`Location`(python/node 均可读) | 已修复 |
| 4-31 | 低 | M | `retry-after-ms` 可解析但负值/非有限时回落 `Retry-After`(python/multipart 短路) | 已修复 |
| 4-32 | 低(文档) | M | D0163 对 python 超时基线表述失实(600s 为逐 IO);Timeout/ResponseBody 不变式未文档化 | 已修复(决策+文档) |
| 4-33 | 低 | N | 验签后 Decode 错误经 `#[source] serde_json::Error` 泄漏 payload 片段(实证) | 已修复 |
| 4-34 | 低 | N | `whsec_` 前缀解码为空钥被静默接受,空钥伪造签名可通过(实证) | 已修复 |
| 4-35 | 低 | N | tolerance 未来侧恰等边界(== 放行)无测试锁定 | 已修复 |
| 4-36 | 低(文档) | N | 防重放目的/对称窗口/时钟同步建议未文档化(node 基线有专节) | 已修复 |
| 4-37 | 中低 | O | Codex `error` 通知通道完全未类型化(TurnError/CodexErrorInfo 11+5 变体为裸 Value) | 已修复 |
| 4-38 | 中低 | O | 子进程崩溃/被杀与正常 EOF 不可区分,`ChildExit` 类别从未用于真实退出 | 已修复 |
| 4-39 | 低 | O | 错误通道零失败路径测试(Rpc 传播/连接广播/EOF 半行/非法 JSON) | 已修复 |
| 4-40 | 低 | O | direct `status_error` 丢弃 error.message;`UnsupportedDirectTransport` 死变体文案失实;`Error::Json` 文案混用+RequestTimeout 无 method;`RpcError.data` 显式 null 丢键;4MiB 帧上限拆连接(D0144 同型) | 已修复 |
| 4-41 | 低 | P | RMCP 桥错误面 7 项:Cancelled 文档不符/cancel 失败被掩盖为 Transport/input_required 归类/UnknownFunction 零测试/list_tools 取消不发通知/Compact 丢 resultType 未记档/无示例 | 已修复 |
| 4-42 | — | 多域 | 决策记录类:4-A2(constraint 错误仅计数)、D-2(chat/completions 干净 EOF fail-stop)、L4-5(BodyPreview 立场) | 已修复(决策条目) |
| 4-R1 | — | C | beta WS 错误事件 Ok 交付的形态立场(4-15 只做最低限度) | 已拒绝(lane 级错误非致命,保留事件交付) |
| 4-R2 | — | M | python 平铺错误体(无 error 键)顶层取字段的形态 | 已拒绝(官方两侧分歧,Rust 站 node 侧) |

## 关键详情(摘要)

- **4-01**:responses.rs:10471(ComputerCall::new)/11919(LocalShellCall::new)/11067(ToolSearchCallInput::status)/11452(ReasoningItem::status)/11987(LocalShellCallOutput::status)——pin 各 item status 均 3 值(`FunctionCallItemStatus` 同域),收窄之;测试表补 5 宿主。
- **4-02**:WebSearchCall(10917-)/FileSearchCall(9772-)/CodeInterpreterCall(11706-)/ImageGenerationCall(11544-,连 result() 都无)/ComputerCall/LocalShellCall(无 call_id())/McpListTools(error 只有 builder)/McpCall(无 status())——补 status()/id() 等只读 getter(python/node 公开属性等价);`required_tagged_record!` 宏可为标量字段生成。
- **4-06**:completions.rs:198-206 `decode_chunk` 直接反序列化,错误帧退化为 "missing field id" 的 Decode;复刻 chat_stream 两段式(先 Value、查非 null error → `StreamError::from_body`)+镜像测试。
- **4-07**:media.rs:459 `SseStreamDecoder::with_default_limits`,5 调用点(90/132/200/236/279)不传 `transport().sse_limits()`;builder 文档 "for Responses SSE streams" 与实际不符。
- **4-17**:tungstenite 0.29 握手失败把响应体放进 `Error::Http`(handshake/client.rs:93-98);`map_websocket_error`(responses_websocket.rs:533-542)只取 status+request_id。`Error::WebSocketHandshake` 加 `body: BodyPreview`(+可用 ApiErrorEnvelope 解析 message);三个 WS 客户端共用映射一次修复。
- **4-18**:`Message::Close(_)` 丢 CloseFrame;`RealtimeInbound::Closed` 携带 `Option<CloseFrame>`+`close_code()/close_reason()` 访问器(或非 1000/1001 报错——选访问器方案,保守)。
- **4-21**:`PollOptions::for_batches()`(5s/24h 对齐 completion_window);`FineTuningJobs::poll`/`EvalRuns::poll`/`Batches::poll` rustdoc 指向预设;poll.rs:1 模块文档补 Batches/Responses。
- **4-25**:admin.rs:2346-2352 `error_from_response` 用 `read_limited`(超限→BodyTooLarge、中断→Transport);改 transport 同款截断语义(`read_up_to` 等价)+读中断统一 `ResponseBody`。
- **4-33**:错误链 `{err:?}`/source Display 含 payload 字面量(SIP 凭据值位置不巧即泄);结构化变体(类别+行列)或脱敏包装;补回归测试。
- **4-37**:pin `v2/ErrorNotification`(threadId/turnId/willRetry)→`v2/TurnError`(message/additionalDetails/codexErrorInfo)→`v2/CodexErrorInfo`(11 字符串+5 对象变体);`decode_notification` 加 "error" 分支;`Turn.error` 收窄 `Option<TurnError>`。
- **4-40**:direct status_error code 缺失回退经清洗的 message;删 `UnsupportedDirectTransport`;`Error::Json` Display 中性化+解码错误附方法名+RequestTimeout 附 method;`RpcError.data` 改 `Nullable<Value>` 三态;`DEFAULT_LINE_LIMIT` 4MiB→32MiB(D0144 同型论证)+文档。
- **4-41**:7 项打包:Cancelled rustdoc 修正(承载对端取消);executor 取消分支忽略 cancel 发送失败优先返回 Cancelled(与超时分支统一);新变体 `UnsupportedResult`(input_required/task)+e2e fixture;UnknownFunction/discover 失败测试;list_tools 取消差异文档;Compact 文档补 resultType/_meta 丢失说明;新增 `examples/rmcp_bridge.rs`。

## 决策记录类(4-42,主代理落档)

1. **CreateResponseConstraintError 仅计数不携带路径**(4-A2):变体刻意不带值(隐私),定位信息以传输侧 serde_path_to_error 的 Decode 路径为准;落档为有意设计。
2. **chat/completions 干净 EOF 无 [DONE] 即报错**(D-2):python/node 静默结束;Rust fail-stop 把静默截断变显式错误,与 D0187 三项强校验同族,补录。
3. **BodyPreview 立场**(L4-5):8KB 上限/键表+前缀双层脱敏/Display 只含 status/code/request_id/Debug 全 REDACTED,vs python `response.text` 全文——安全姿态落档。
4. **Realtime WS 握手有意单发**(4-20):node/python 均不重试 WS 连接;Responses WS 的 InitialConnect 为 opt-in 面,Realtime 暂不提供,记录不对称理由。
5. **铸造类 POST × 自动重试**(4-26):三个一次性 value 端点默认 Replayable 与 python 平价;风险(孤儿凭据)与逃生门(conservative/disabled)落 rustdoc+决策。
6. **D0163 措辞更正**(4-32):python 600s 为逐 IO 超时非总预算;本仓 request_timeout 为总预算(采纳 node 侧),流式长流会在 600s 切断——更正表述并补 Timeout/ResponseBody 不变式文档。

## 结论

流式/错误通道镜头下:2 项中低+8 项低的核心错误通道缺口(错误体丢弃/错误帧漏检/访问器缺失),其余为测试/文档/一致性;3 项回归(4-01/4-16/4-10)。2 项已拒绝。第 3 轮修复回归其余全部通过。

## 修复记录(2026-08-31 执行完毕)

- 42/42 项闭环:4-01~4-41 代码/文档修复(约 60 个新测试函数),4-42 六项立场以 D0199/D0208 落档。决策条目 D0189–D0208(20 条)已追加 `spec/contracts/decisions.md`(账本累计 208 条)。
- 修复分组:组1 responses/beta 访问器与收窄(4-01/02/03/04/05/14/15/16)、组2 client 流错误(4-06/07/08/10/11/12/13,代理汇报超时但改动完整落盘,经主代理逐项抽查确认)、组3 错误对象(4-09/27/28/29/30)、组4 WS/admin 传输(4-17/18/19/20/25/26/31)、组5 evals/voices/VS(4-22/23/24 types 侧)、组6 poll(4-21)、组7 webhooks(4-33~36)、组8 codex(4-37~40)、组9 rmcp(4-41)。
- 主代理收尾:4-24 client 侧(新增 `Error::RequestPayloadTooLarge` 变体并在 `prepare_bounded` 使用);组2 完成度核验;组4 的 error.rs 最小越权改动(WebSocketHandshake.body)复核认可。
- 全部门禁通过:fmt、clippy(-D warnings 零错)、全量测试 **856 passed / 0 failed**、no-default-features、xtask check、cargo deny。
- 遗留:无新待定项。large_enum_variant Box 化工程债仍开放(非 wire);下轮候选——beta Responses WS 套接字未同步 4-18/4-19 的 close code/reason 与失败退役语义(审查观察 7)。
