# 问题16.md — 第 16 轮复扫(R6 Admin 深扫 + 实验面/横切镜头)

- 日期:2026-09-02
- 轮次:16 / 20(R6 Admin 深扫 + 实验面;16 域,零限流)
- 基线:同前
- 总体:**Admin 域六族深扫全部零发现**(api-keys/users、projects/invites、roles/groups、audit logs、usage×11、certs/spend/settings——122 op 机器双向比对、DTO 逐字段机器 diff、147 事件/55 载荷键/120 字段全锁);chatkit、弃用面、安全面亦零发现;发现集中在数值类型、codex 分支前向兼容、rustdoc 健康与 client-config 立场缺口

## 汇总表

| 编号 | 严重度 | 领域 | 问题 | 状态 |
|---|---|---|---|---|
| 16-16-1 | 中 | 文档 | `cargo doc -D warnings` 12 处 intra-doc 链接错误横跨 3 crate(2 处已知 codex + 10 处新发现:admin operations 链接域、beta/realtime 私有项、宏生成 Unknown 链接) | 已修复(D0281)|
| 16-14-1 | 低 | 配置 | 双 SDK 均有 `http_client`/`httpAgent`/`fetch` 注入而我们没有且无落档(池调优/自定义连接器/mock 传输被封死) | 已落档(D0282) |
| 16-14-2/3/4 | 信息 | 配置 | default_headers/default_query、REST 每-请求 extra_headers、with_options 泛化——均有双 SDK 基线、无落档 | 已落档(D0282) |
| 16-11-1 | 低 | 特性 | openai-rs-rmcp `use std::time::Duration` 未按 client 特性门控——server-only/auth-only 构建告警(gate 组合从未编译过) | 已修复(D0281)|
| 16-15-1 | 低 | 数值 | `TranscriptionStreamLogprob.bytes: Vec<u8>` 夹逼 pin 无界 integer(D0269 遗留裁决:应为 `Vec<i64>`;非流式面 pin 是 number,f64 正确保留) | 已修复(D0281)|
| 16-15-2 | 低 | 数值 | chat `ChatTopLogprob`/`ChatTokenLogprob`.bytes `Nullable<Vec<u8>>` 同病(D0255 只仲裁了 null 项,未涉元素类型) | 已修复(D0281)|
| 17-O-1(codex) | 低 | codex | SandboxPolicy/AskForApproval 已知分支解码静默丢弃 pin 合法附加子键(无 flatten extra,ThreadItem 分支均有) | 已修复(D0281)|
| 17-O-2(codex) | 低 | codex | pin 必需的 `Thread.cliVersion` 未建模(13-O-1 类复发) | 已修复(D0281)|
| 16-信息组 | 信息 | 多域 | 15-3(非流式 f64 裁决闭合)、15-4(整数符号一致性)、13-1(live Client 凭据轮换立场)、O-3(fake-child 缺 D0267 字段)、rmcp 缓存零 e2e(17 轮补)、rmcp mcp_tool 承诺措辞、1/2/3/4/5/6/9/10/12 各信息注记 | 已落档(D0282) |

## 零发现域

- **Admin 六族**(16-1..6):19+46+48 op 枚举比对、usage 11 端点行 DTO 机器 diff、audit 55 键/120 字段/147 事件、certs/spend/data-retention 全对;3 个富余绑定即 checkpoint-permission 三元组
- **chatkit**(16-9):6 op/头/限值/ThreadItem 联合全对;**弃用面**(16-10):assistants/videos/variation 隔离均有 D0013 落档,beta-realtime 车道 D0280 已落;**安全面**(16-12):flat 错误车道脱敏同嵌套、累加器无敏感物、span 全景与 D0231+D0278 精确一致、示例/fuzz 无密钥

## 修复分工

- 主代理:16-11-1(一行 cfg)、16-15-1/2(三处 i64 + 两个回归测试)——已完成
- 代理A(codex protocol.rs):17-O-1、17-O-2
- 代理B(rustdoc):16-16-1 全部 12 处
- 主代理落档:D0281+

## 结论

R6+实验面:Admin 域在两轮机器校验(D0249/D0279)加持下全面零发现;残余为 codex 分支前向兼容补齐(D0267 延伸)、logprob 字节整数化(D0269 裁决落地)、rustdoc 纳入门禁的清理、与 client-config 旋钮的立场化。

## 修复记录(2026-09-02)

主代理直接执行 16-11-1(Duration cfg 门控)、16-15-1/2(三处 bytes → i64 + 两个越界回归测试);代理A 完成 codex 17-O-1(四分支+Granular flatten extra,DangerFullAccess 变结构变体)+17-O-2(cliVersion);代理B 修完 12 处 rustdoc 链接(cargo doc -D warnings 全绿)。D0281/D0282 落档。
