# 问题7.md — 第 7 轮对比审计(镜头:实验面 + 七轮覆盖后残余深挖)

- 日期:2026-08-31
- 轮次:7 / 20(16 领域:codex/rmcp 实验面深挖 + 各域前六轮未覆盖角落)
- 基线版本:同前 + WHATWG SSE、RFC 6749/8252、node Buffer.from 实证
- 第 6 轮修复回归:16 域全部通过;发现 1 处回归(7-02:默认特性集 dead_code)

## 汇总表

| 编号 | 严重度 | 领域 | 问题 | 状态 |
|---|---|---|---|---|
| 7-01 | 中 | W | workload identity token 交换内联不可取消安全:取消后单飞槽位永久泄漏,后续所有请求挂死 | 已修复 |
| 7-02 | 中低 | F | 第 6 轮回归:默认特性集下 `http_request_span`/`execute_optional_json_with_static_header` dead_code 警告;门禁无默认特性 clippy | 已修复 |
| 7-03 | 中 | C/O/T | codex direct 120s 硬编码总超时截断流式长回合(同机制 client 侧 600s 有旋钮有落档) | 已修复 |
| 7-04 | 中低 | W | webhook HMAC 工作量线性于候选数(≤182×16MiB≈2.9GiB/请求),node >32 候选走"一次签名+常量比较"(总工作量≤32 次 HMAC) | 已修复 |
| 7-05 | 中低 | O | `v2/ThreadItem` 18 分支 tagged union 完全未类型化(item 内容面全 raw Value) | 已修复 |
| 7-06 | 中低 | O | login 三方法缺 apiKey 分支且 request/request_value 私有,该分支不可达 | 已修复 |
| 7-07 | 中低 | T | codex direct SSE 4MiB 硬编码与 D0144/D0226 立场相悖;lone CR/BOM/[DONE] 三处规范边缘分歧;解码失败后流继续(违反 fail-stop) | 已修复 |
| 7-08 | 中低 | T | WS `InitialConnect` 重连:429/5xx 握手拒绝不重试(仅 Io/Tls);参数语义零文档零测试 | 已修复 |
| 7-09 | 中低 | E | keepalive 静默窗口以陈旧锚点判死:recv 暂停 ≥ 窗口后恢复,首个 tick 立即超时退役健康连接且期间零探测 | 已修复 |
| 7-10 | 中低 | A | admin 数组查询编码 repeat 与两官方 SDK 运行时 brackets 及 pin 自带 `[]` 参数名双重偏离(D0059 证据基线缺失) | 已修复 |
| 7-11 | 低 | R | 资源→输入转换三条路径对 created_by 等 resource-only 字段行为分叉(丢/留/透传并存) | 已修复 |
| 7-12 | 低 | R | `ConversationMessage::to_response_input_item` user/system/developer 分支丢弃顶层 extra(assistant 分支保留,不一致) | 已修复 |
| 7-13 | 低 | R | accumulator 的 part-level 聚合(content_part.added/done、annotation.added 顺序链)无回归测试 | 已修复 |
| 7-14 | 低 | D | legacy typestate decode 侧缺 stream_options/best_of 无效组合守卫(静默丢弃→round-trip 丢字) | 已修复 |
| 7-15 | 低 | B | `BetaAgent` 叶子无 ExtraFields(未知 agent 元数据字段静默丢失);`validate_stream_id` 两模块字节级重复 | 已落档(D0244,记录性不改) |
| 7-16 | 低 | M | 图像流式 completed 终止事件无 client 级测试;SpeechVoice::Custom 无构造/桥接 | 已修复 |
| 7-17 | 低 | H | `Uploads::add_part` 无 >64MB 本地拦截(长度 prepare 时冻结可知);typed 行 body 可为非对象(服务端异步判死);`BatchSubmissionError::Io` 文档与归类不符 | 已修复 |
| 7-18 | 低 | I | `VectorStoreSearchQuery::Texts(vec![])` 可经变体直接构造(minItems 只挡构造器与 decode);容器 multipart upload 无法发送 schema 可选 `file_id` | 已修复 |
| 7-19 | 低 | J | alpha grader 端点 DTO 双轨:FT 侧 `token_usage: Nullable<u64>` 无法解码 pinned 官方示例(对象形态),evals 侧正确 | 已修复 |
| 7-20 | 低 | K | AuditLog 147 事件与 55 payload 的 parity 仍自引用(6-15 只覆盖操作 manifest) | 已修复 |
| 7-21 | 低 | L | ExtraFields reserved-key 防护 API 全仓零调用(死代码+文档误导);codex 31 处 pub extra 无键冲突防护;structured 对 items 数组/anyOf 非数组形态静默跳过;EncodedEmbedding 无解码辅助 | 已修复 |
| 7-22 | 低 | T | 三 WS 客户端 `send` 传输失败不 retire socket;URL 派生三重复制且 query 处理不一致(GA/beta 清除 base query,realtime 保留) | 已修复 |
| 7-23 | 低 | T/M | `with_request_timeout` 预算不覆盖 auth 获取且不传播 workload 交换;流式方法"握手后零重试"未文档化 | 已修复 |
| 7-24 | 低 | P | `experimental-codex-direct` 启用后门面零暴露(别名仅 cfg codex-app-server);pub API rustdoc 覆盖 codex 38%/client 69% | 已修复 |
| 7-25 | 低 | M | rmcp 取消分支 `handle.cancel().await` 无界(写方向卡死时 dispatch 越过 deadline 永久挂起);工具适配丢弃字段未文档化;trait 契约不完备;分页发现零 e2e | 已修复 |
| 7-26 | — | 多域 | 决策落档类:base64 strict-canonical 立场、secret 中间缓冲窗口、direct 超时立场、WS send 姿态 | 已修复(决策条目) |
| 7-R1 | — | C | conversations include 语义等 | 已确认一致(无问题) |

## 关键详情(摘要)

- **7-01(中)**:workload_identity.rs:418-426 `token()` 内联 exchange 后 `finish_refresh`;调用方 future 被 drop(外层 timeout/select 常见)→ `refreshing` 槽位残留 → 后续所有 `token()` 走 `notify.notified().await`(无超时)永久阻塞;x509.rs:616-627 的 `tokio::spawn` 分离任务是仓内正确范式。修复:对齐 x509(spawn 分离任务)或 Drop guard。
- **7-02(回归)**:trace.rs:39 `http_request_span` 剩余调用方全在非默认特性;transport.rs:211 唯一调用方 beta_responses。修复:cfg 门控或统一 lazy 后删除;门禁补默认特性 clippy;修正 development.md 声明。
- **7-03(中)**:direct/transport.rs:51 `.timeout(120s)` 覆盖流式 body 全程;无参数无文档。修复:超时旋钮(流式 lane 仅连接/空闲超时或大预算);补决策。
- **7-04(中低)**:webhooks.rs:183-196 逐候选全量 HMAC;node >32 走一次 sign+XOR 比较。修复:镜像 node(一次期望 HMAC+逐候选常量比较+选中候选一次 verify);语义(D0225 接受全部候选)不变。
- **7-05(中低)**:pin `v2/ThreadItem` 18 分支;`ItemLifecycleNotification.item: Value`。修复:开放 tagged enum(高频分支类型化,未知 tag 降级 Value);`Turn.items` 复用。
- **7-09(中低)**:realtime.rs:795-831 tick 分支先判后探;`last_inbound` 锚定 connect/上一帧。修复:recv 入口检测轮询间隙(≥ping_interval 未被 await)则重置锚点;补暂停-恢复回归测试。
- **7-10(中低)**:admin.rs:2598-2602 repeat vs python client 级 Querystring(brackets)/node qs brackets;pin audit 五个参数名自带 `[]`。修复:audit 五过滤器按 pin 拼写发 `[]`;其余数组参数对齐基线 brackets 或落档;修正注释;更新测试。
- **7-19**:fine_tuning.rs:769-811 `token_usage: Nullable<u64>` vs 官方示例对象形态;evals::experimental 的 `GraderTokenUsage` untagged 正确。修复:FT 侧删除未接线端点 DTO 或复用 evals 类型;落档单轨立场。
- **7-25**:executor.rs:122/127 cancel await 无界。修复:短 timeout 包裹(1s)后照常返回;补挂起写回归测试;`function_tool` 补丢弃清单文档;trait 补 control 义务/错误分类指引;ProbeServer 分页+发现超时 e2e。

## 决策落档类(7-26)

1. base64 strict-canonical(候选与 whsec_ 仅标准填充;node 宽容是 Buffer.from 副作用)。
2. secret 构造中间 String 缓冲不经 zeroize(secrecy From<String> 语义;exact drop 成立)。
3. direct 120s(修复后为旋钮默认值)与流式语义。
4. WS send 失败的 socket 姿态(随 7-22 修复落档)。
5. GA/beta WS 清除 base query vs realtime 保留(统一立场)。

## 结论

实验面+残余镜头:1 中(workload 取消泄漏)+ 8 中低 + 14 低 + 5 决策;第 6 轮修复回归 16/16 通过,1 处回归(7-02);codex direct 与 workload identity 是本轮两大实质发现区。

## 修复记录(2026-08-31 执行完毕)

- 26/26 项闭环:7-01~7-25 代码/文档修复(约 50 个新测试函数),7-26 六项立场以 D0244 落档。决策条目 D0234–D0244(11 条)已追加 `spec/contracts/decisions.md`(账本累计 244 条)。
- 修复分组:组1 workload+回归(7-01/02)、组2 webhooks HMAC+admin brackets(7-04/10,代理汇报超时但改动完整落盘并经主代理核验:一次签名+XOR 常量比较+选中候选一次 verify;audit 五过滤器 `[]` 拼写+admin 数组 brackets)、组3 codex direct(7-03/07)、组4 keepalive/WS(7-08/09/22)、组5 codex 类型化(7-05/06)、组6 Responses/Chat 残余(7-11~14)、组7 media/files/FT-VS(7-16~18)、组8 核心/admin/rmcp(7-19~25)。
- 主代理核验:组2 的 HMAC 实现逐行确认(XOR-fold 无短路、8KiB 界保留、D0225 语义不变);admin brackets 的测试断言与注释确认;两组超时代理的完整性经 git diff 复核。
- 全部门禁通过:fmt、clippy(-D warnings 零错;**默认特性集与最小特性集零警告**——第 6 轮回归已修)、全量测试 **1006 passed / 0 failed**、no-default-features、xtask check、cargo deny。
- 遗留:7-10 平台侧数组风格(python client 级 brackets 同样适用于 platform 通道,但无 pin 参数名带 `[]`,需维护者决策+线上确认——D0244 记录);validate_stream_id 双份未去重(D0244)。
- 审查整改:第7轮审查有条件通过,三项整改已落实——7-23 两处 rustdoc 补齐(create_stream 终态语义、with_request_timeout 预算范围);7-15 状态列改为"已落档(D0244,记录性不改)";遗留段编号 7-23→7-10 更正。
