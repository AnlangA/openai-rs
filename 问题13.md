# 问题13.md — 第 13 轮复扫(R3 镜头:枚举/判别联合第二遍)

- 日期:2026-09-01
- 轮次:13 / 20(R3 枚举/判别镜头;16 域全量,2 域因账户限流重派后完成)
- 基线:同前(OpenAPI pin rev 690521b、openai-python b19c2161、openai-node eea2292a、codex 0.144.5、rmcp 3.1.4)
- 总体:变体集/拼写层面高度收敛(58/58 Responses 流事件、46/46 Realtime 服务端事件、28/28 Conversation item、147/147 Admin 审计事件、61/61 Beta 事件逐一集合相等);发现集中在**判别联合的 Unknown 保留缺口**与**嵌套 union 的前向兼容**两类

## 汇总表

| 编号 | 严重度 | 领域 | 问题 | 状态 |
|---|---|---|---|---|
| 13-A-1 | 中 | A | `ResponseInputItem` 解码表无 `"item_reference"` 臂:pin 成员 `ItemReferenceParam` 的显式 tag 形态落入 Unknown,typed 变体不可达(responses.rs:4687-4801) | 已修复(D0256) |
| 13-A-2 | 中 | A | 输入 item 显式 `"type": null` 硬错误整个解码;pin 的 ItemReferenceParam 分支 `anyOf[enum,null]`、python/node 均接受显式 null(同修复点) | 已修复(D0256) |
| 13-I-1 | 中 | I | `VectorStoreExpirationAfter` 带 `deny_unknown_fields` 但 pin 无 `additionalProperties:false`;作为响应 DTO,未来 expires_after 新字段会使整个 VectorStore 解码失败;容器类比 ContainerExpiration 用 ExtraFields(vector_stores.rs:602-607) | 已修复(D0262) |
| 13-L-1 | 中 | L/B/D/F | 四个 moderation 判别 union(ChatModerationClassification、ChatCompletionModerationOutcome、ResponseModerationOutcome、BetaModerationOutcome)为闭合 `#[serde(tag="type")]`,无 Unknown 兜底、变体无 ExtraFields;未来 outcome tag 使整个 Response/ChatCompletion 解码失败,而同文件其余 union 均降级 Unknown;python 变体 extra=allow | 已修复(D0258) |
| 13-O-1 | 中 | O | codex `ThreadStartResponse` 丢弃三个 pin 必需字段 approvalPolicy/approvalsReviewer/sandbox(+可选 reasoningEffort)于 extra;协商出的审批/沙箱姿态不可读(protocol.rs:872-887) | 已修复(D0267) |
| 13-O-2 | 中 | O | codex `Thread.status`(4 分支 union: notLoaded/idle/systemError/active{activeFlags},含 waitingOnApproval/waitingOnUserInput)及 source/threadSource 未建模,生命周期状态机仅存于 extra(protocol.rs:833-856) | 已修复(D0267) |
| 13-A-3 | 低 | A | 闭合过滤结构(McpToolFilter/McpApprovalFilter/WebSearchFilters/ModerationDirection/PromptCacheBreakpoint)缺 ExtraFields;McpAllowedTools 无 Unknown 分支——未来字段静默丢弃 | 已修复(D0257) |
| 13-C-1 | 低 | C | 30/58 Responses 事件 DTO 有 flatten extra 但无 `extra_fields()` 访问器;`McpCallArguments{Delta,Done}Event` 缺 item_id/output_index/sequence_number 访问器(读面一致性,D0190 家族复发) | 已修复(D0259) |
| 13-E-1 | 低 | E | Realtime 三个嵌套闭合 union 未知判别值即错误:TranscriptionUsage(:2508)、Truncation 对象形(:1130)、ConversationItem message 未知 role(:2210)——与模块"未知 tag 保留"承诺不一致(python/node 同样闭合,无线上行为分歧) | 已修复(D0260) |
| 13-F-1 | 低 | F | `BetaMultiAgentOutputText` 请求侧必发 `logprobs:[]`/`annotations:[]`;pin 的 `BetaOutputTextContentParam` 无 logprobs 字段,python/node 均不发(beta_responses.rs:1536-1547) | 已修复(D0261) |
| 13-J-1 | 低 | J | Conversation 资源构造器收 8 值状态超集(D0169 只收窄了请求侧构造器,漏掉这两个资源构造器;经 to_response_input_item 回放可产出 pin 非法 status) | 已修复(D0263) |
| 13-M-1 | 低 | M | retry 兜底 `is_server_error()` 止于 599;python `_base_client.py:837-839` 与 node `client.ts:1604-1606` 均为 `>= 500`(非标准 6xx 不重试,fail-closed) | 已修复(D0264) |
| 13-M-2 | 低 | M | `ApiError` 只解嵌套 `{"error":{…}}` 信封;python `_client.py:834` 为 `body.get("error", body)` 平面对象兜底;与自家 StreamError 的 flat-then-nested 不对称 | 已修复(D0265) |
| 13-N-1 | 低 | N | D0250 决定的 webhooks 模块文档出处更正(交付语义引官方平台指南而非 node docs)从未落地,client webhooks.rs:4-15 仍错误归属(node docs 无此内容,仅链接平台指南) | 已修复(D0266) |
| 13-O-3 | 低 | O | codex `Turn.itemsView`(notLoaded/summary/full)未建模,落入 extra(protocol.rs:2273-2294) | 已修复(D0267) |
| 13-P-1 | 低 | P | facade rmcp 别名无法命名 `ServerSink`/传输类型,而 lib.rs:52-57 记录的缓解方案引用了不可达的 `StreamableHttpClientTransport`(openai-rs-rmcp 只再导出 4 个模型类型) | 已修复(D0268) |
| 13-信息组 | 信息 | 多域 | 落档:B-2/B-3(status_details 不存在于 GA pin;8 值状态超集既定)、C-2(ChatToolKind::Custom 前置 pin-lag,node 已有)、D-1/2/3、F-2/3、G-1..5、H-1/2/3、J-2/3、L-2/3/4、M-3/4([DONE] 精确匹配、UA 组成)、N-2/3、O-4/5、P-2/3/4、E-2、K 零发现 + settings/batch-users 不在 pin | 已落档(D0269) |

## 零发现域与大面积收敛证据

- **K/Admin**:机器双向比对(344 响应属性位 + 26 请求/查询域)——pin→Rust 零缺失、Rust→pin 零幻影;AuditEventType 147=147
- **G/Media**:22 个枚举 letter-for-letter;语音模型集修正为 tts-1/tts-1-hd/gpt-4o-mini-tts(+dated),speech pcm 非 pcm16
- 事件判别面:Responses SSE 58/58、Beta 61/61、Realtime server 46/46 + client 11/11 + translation 7/3、Conversation item 28/28 逐一集合相等;`[DONE]`/终态表与官方一致
- 数值化发现(非本镜头):TranscriptionStreamLogprob.bytes 为 Vec<u8> 而 pin 为 integer(>255 会解码失败)——记入下轮数值镜头候选

## 修复分工(按文件所有权,两波)

- 波1-组1(types/responses.rs):13-A-1、13-A-2、13-A-3、13-L-1 响应侧、13-C-1
- 波1-组2(types/chat.rs + beta_responses.rs):13-L-1 chat 侧×2 + beta 侧、13-F-1
- 波1-组3(types/vector_stores.rs + conversations.rs):13-I-1、13-J-1
- 波1-组4(types/realtime.rs):13-E-1
- 波2-组5(client: transport.rs + error.rs + webhooks.rs):13-M-1、13-M-2、13-N-1
- 波2-组6(codex protocol.rs):13-O-1、13-O-2、13-O-3
- 波2-组7(openai-rs-rmcp):13-P-1
- 主代理:信息组落档(D0256+)

## 结论

R3 复扫:变体集与拼写全面收敛(官方 SDK 拦不住的未知值 Rust 均无损);残余缺口集中在(a)少量手写闭合判别 union 缺 Unknown/ExtraFields 兜底,(b)pin 必需字段仍藏在 extra 的接收面(codex Thread 家族),(c)两处与 python 的行为分歧(6xx 重试边界、平面对象错误兜底)。

## 修复记录(2026-09-01)

7 个修复代理按文件所有权两波并行,全部完成;D0256-D0269 落档:

| 编号 | 修复内容 | 落档 |
|---|---|---|
| 13-A-1/2 | `Some("item_reference")` 解码臂映射现有 ItemReference(flatten 保留 type 键,字节等价往返);`Some(Value::Null)` 剥离后按缺省判别路由;`"<absent:id>"` 标记替换为 `"item_reference"`(32/32 位置对齐,grep 证实无编码消费者) | D0256 |
| 13-A-3 | 五个闭合过滤结构补 `#[serde(default, flatten)] extra` + `extra_fields()`;PromptCacheBreakpoint 弃 Eq/const、McpRequireApproval 按惯例 allow large_enum_variant | D0257 |
| 13-L-1 | 四面 moderation union 全部改 tagged-union 惯用法:载荷结构体(公共字段+flatten extra+extra_fields())+ Unknown 兜底;GA 与 chat 两代理独立收敛出同形 | D0258 |
| 13-C-1 | 三个事件宏 + 8 个手写事件补 `extra_fields()`;两个 MCP arguments 事件补 item_id/output_index/sequence_number | D0259 |
| 13-E-1 | Realtime 三处 fall-through 路由 UnknownRealtimeObject(usage 未知 kind/未来 truncation 策略对象/未知 role→Unknown 臂);已知的 0..=1 约束保持 opt-in | D0260 |
| 13-F-1 | 新增 `BetaMultiAgentOutputTextParam`(annotations 空则省略、无 logprobs)仅用于请求侧;资源面与 pin 测试不变;回显解码容忍但不重发 logprobs | D0261 |
| 13-I-1 | 弃 deny_unknown_fields,公开/线结构补 flatten extra + extra_fields();anchor/days 保持 pin 必需;1..=365 构造与解码双重生效(check_days) | D0262 |
| 13-J-1 | 两个 Conversation 资源构造器收窄为 per-host MessageStatus/FunctionCallItemStatus(D0169 补全);解码面保持 8 值超集 | D0263 |
| 13-M-1 | 兜底改 `as_u16() >= 500`(引证 python `_base_client.py:851-853`、node `client.ts:1606-1607`);真值表补 499/600/999 及 600×header-override | D0264 |
| 13-M-2 | `ApiError::from_body` 镜像 StreamError 的 flat-then-nested 优先级(平面对象先解,嵌套只补缺);标准信封行为不变 | D0265 |
| 13-N-1 | webhooks 模块文档交付语义句改锚官方平台指南(node docs 仅链接之;两处有据的方法级引证保留) | D0266 |
| 13-O-1/2/3 | ThreadStartResponse 补四字段(Option 容错);Thread 补 status(4 臂开放联合+activeFlags)/source/threadSource(pin 必需性核实:source 必需、threadSource 可选);Turn.items_view 开放枚举+默认 full 访问器;AskForApproval/SandboxPolicy 增 Unknown(Value) 无损臂 | D0267 |
| 13-P-1 | `pub use rmcp;`(D0231 reqwest 先例);缓解文档改写为已核实的可命名路径;两个可命名性测试 | D0268 |
| 13-信息组 | 约 25 项立场落档(ChatToolKind::Custom 前置、[DONE] 精确匹配、UA 组成、批处理输出行双键、TranscriptionStreamLogprob.bytes 记为下轮数值镜头候选等) | D0269 |

新增测试约 20 个(input_item_reference/null-type、closed_filter、moderation×4、stream accessors、realtime nested、beta param、VS future-member、conversation per-host、retry 真值表扩展、flat error×2、codex×6+调整 3、rmcp×2)。代理侧验证:types 518 通过、client 440、codex 119、rmcp 36,各自 crate 级 clippy/fmt 干净。
