# 问题15.md — 第 15 轮复扫(R5 镜头:分页/查询/HTTP 语义第二遍)

- 日期:2026-09-02
- 轮次:15 / 20(R5 分页/HTTP 镜头;16 域全量,零限流失败)
- 基线:同前
- 总体:查询参数/游标/状态码门控全面收敛——B/C/D/G/I/K 六域零发现(含空容器、bool/float 序列化、路径编码、重定向、头部卫生、JSONL 帧、直接通道等全轴对齐);发现集中在**一处第 14 轮机械遗漏**、**两个便利面缺口**与**机器校验缺口**

## 汇总表

| 编号 | 严重度 | 领域 | 问题 | 状态 |
|---|---|---|---|---|
| 15-O-1 | 中 | O | facade 漏配第 14 轮新增的 `ChatCompletionAccumulator` 再导出(client crate 根已导出,facade lib.rs 无)——facade 用户无法命名 D0270 累加器 API | 已修复(D0279)|
| 15-A-1 | 低 | A | 空 data 页+has_more=true+非空 last_id:python `next_page_info` 于 data 空时静默停止,我们继续翻页——未落档的边界分歧(D0147/D0276 未覆盖) | 已落档(D0280) |
| 15-E-1 | 低 | E | spend-alert 两列表 facade 方法无参签名,pin 的 limit/order/after/before 四参不可达(admin 域唯一"pin 有分页参而 facade 丢弃"的列表;泛型面可用故低) | 已修复(D0279)|
| 15-F-1 | 低 | F | `ListEvalRunOutputItemsParams::status` doc 杜撰 "error" 滤值(pin 枚举仅 fail/pass) | 已修复(D0279)|
| 15-H-1 | 低 | H | python 弃用 beta-realtime WS 车道(`OpenAI-Beta: realtime=v1`)不可达;realtime 面无 header 逃生口(node 已弃 WS 面,无双 SDK 共识) | 已落档(D0280) |
| 15-L-1 | 低 | L | rmcp 缓存可在一次 list_tools 内合并"缓存首页+live 后页"的跨代目录(DuplicateToolName/漏工具),D0277 只披露了单页新鲜度 | 已修复(D0279)|
| 15-P-1 | 低 | P | 手写查询 DTO 与 manifest 的 71 op/301 查询参数无任何机器 parity(重命名/缺失字段可编译并通过 xtask check);drift.rs 丢弃参数位置与状态码变化 | 已修复 admin 侧(D0279),全量后续落档|
| 15-信息组 | 信息 | 多域 | A-2/3(order 开放枚举、DELETE 204 容忍)、C-1/2(upload 部件两种载运、first/last_id 必需)、D-1/2(VS next_page 不可用即三方一致、游标矩阵)、G-1/2(翻译 Accept 超集、双内容覆盖)、I-1..5、J-1/2/3(python 静停对比、页流 API 形态立场、limit 表)、K-1..4(direct 401 预算×2、content-type 不对称、arguments 封存、session_id 每次新生)、M-1..4(多值头 join 分歧、时间戳严格性、node 空 id 签名基、无签名助手=一致)、N-1..4(background 已 server-paced、count-tokens 面、retrieve_stream 精确、conversations 覆盖)、O-2/3(无分页示例、无迁移文档)、P-2/3/4 | 已落档(D0280) |

## 零发现域与收敛证据

- **B/Chat+legacy+embeddings**:chat 列表五参(model/metadata 深对象/after/limit/order)逐字一致;embeddings 四分支 input+encoding_format;base64 解码;pin 无 deprecated 遗留
- **C/Files/Batches/Uploads**:files 四参/batches 两参逐字一致(默认 10000/20);上传部件 multipart 载运即 pin 声明;下载经 files/content;DELETE 语义正确
- **D/FT/VS/Containers**:游标样式矩阵干净(after×全量,next_page 仅 VS POST 结果且三方一致不可用);order 拼写差异(asc/desc vs ascending/descending)逐处对齐
- **G/Media**:内容协商逐 op 对齐 pin 的 content map(双内容 edit 拆两 lane);Accept 按格式表驱动;multipart 字段拼写/省略/部件 content-type 全对
- **I/Transport 查询核心**:空容器/bool 小写/float/路径编码/重定向/头部卫生全轴 parity;状态码门控扫描 pin 仅 200/201,三处 201 正确
- **K/Codex**:JSONL 帧(写出即 flush、32MiB 双向、stderr 泄流)、env 白名单、id 唯一性、超时预算全清;direct 通道密封姿态一致

## 修复分工

- 主代理直接执行(量小):15-O-1(facade 一行)、15-E-1(spend-alert list 加参)、15-F-1(doc 一词)、15-L-1(rmcp doc 一句)、15-P-1(admin 投影扩展 params 列)
- 主代理落档:D0279+(15-A-1/15-H-1 立场 + 信息组 + P-2/3 后续)

## 结论

R5 复扫:查询/分页/HTTP 层面实质收敛(六个整域零发现);残余为第 14 轮 facade 机械遗漏、两个便利面缺口(spend-alert 分页、realtime 弃用车道立场)与查询参数的机器校验缺口(以 admin 投影扩展起步)。

## 修复记录(2026-09-02)

本轮修复量小,主代理直接执行 + 1 个测试代理:

| 编号 | 修复内容 | 落档 |
|---|---|---|
| 15-O-1 | facade lib.rs 补 `ChatCompletionAccumulator` 再导出 | D0279 |
| 15-E-1 | 两个 spend-alert `list` 增加 `&AdminListParams` 参数(builder 链 `query(params)?`,裸列表 = default;测试调用点更新) | D0279 |
| 15-F-1 | evals status doc 去掉杜撰的 "error" 滤值 | D0279 |
| 15-L-1 | rmcp 缓存文档补跨页混代危害句(禁用缓存逃生口同除) | D0279 |
| 15-P-1 | 新增 `admin_query_parameter_names_match_the_pinned_manifest`:manifest 35 个带查询参的 admin op 逐名(归一 `[]` 后)断言可由 AdminListParams/AuditLog/Usage/Certificate/ProjectGroup 参数 DTO 序列化,op 数量断言防 manifest 收缩;全仓 per-resource 横幅与 P-2/P-3 落档为后续 | D0279/D0280 |
| 15-A-1/15-H-1 | 空 data 页继续翻页 vs python 静停、beta-realtime 弃用车道不暴露——立场落档 | D0280 |

验证:fmt/clippy 绿,admin+types+facade 目标测试全绿(含新 parity 测试);六域零发现(B/C/D/G/I/K)不需修复。
