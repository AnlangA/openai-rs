# 问题6.md — 第 6 轮对比审计(镜头:Admin 全量 + 五轮修复回归 + tracing 新维度)

- 日期:2026-08-31
- 轮次:6 / 20(Admin 119+3 操作分三片逐字段深查;全部领域五轮修复回归;远端 b6b01a6 引入的 tracing 作为新审计维度)
- 基线版本:同前 + OTel HTTP semconv
- 第 5 轮修复回归:16 域全部通过,无回归
- 特记:远端协作者活跃(b6b01a6 tracing),rebase 冲突已妥善解决

## 汇总表

| 编号 | 严重度 | 领域 | 问题 | 状态 |
|---|---|---|---|---|
| 6-01 | 中低 | N | webhook 签名候选 >32 整体拒绝,node 明确测试"第 33 槽有效签名仍接受"(有界验证 vs fail-stop 分歧) | 已修复 |
| 6-02 | 中低 | O | Codex ThreadStartParams 缺 5 个 pin 可选字段、TurnStartParams 缺 5 个(含 turn 级 approvalPolicy),且是唯二无 extra 逃生门的 DTO | 已修复 |
| 6-03 | 中低 | O | Codex 写阶段超时取消可留半帧,连接不 fail-stop(后续帧错位) | 已修复 |
| 6-04 | 低 | K/A | D0145 游标空串家族第三例:AdminNextPage::next_cursor、UsageResponse::next_page、ListFineTuningCheckpointPermissionResponse::next_after(+events/checkpoints 同形) | 已修复 |
| 6-05 | 低 | F | beta PromptCached 消息 content 构造域可走私 computer_screenshot(D0167 beta 侧缺口) | 已修复 |
| 6-06 | 低 | G | ReplayableMultipartSource Debug 明文 file_name/media_type(Path 源 basename 即被脱敏 path 的内容;OneShot 侧已脱敏) | 已修复 |
| 6-07 | 低 | O/L | Codex 27 个 `pub extra: Map` 携带者 derive Debug 明文(与 types 侧 ExtraFields 脱敏姿态不一致) | 已修复 |
| 6-08 | 低 | I/J | containers 两个 list limit 连 0 都放行(D0217 家族第三例) | 已修复 |
| 6-09 | 低 | E | translation secret lifetime 硬解码拒绝 vs GA 同一 pin schema 走 opt-in(u16 还截断) | 已修复 |
| 6-10 | 低 | E | 三个 WS `close_reason()` 文档承诺非空才 Some,实现返回 Some("")(coded close 无 reason 是常态) | 已修复 |
| 6-11 | 低 | D | Chat `moderation.policy` 无型化裸 Map(Responses 侧同 schema 已定型,宿主分叉) | 已修复 |
| 6-12 | 低 | L | `$ref: "#/"`(根引用第二种拼写)绕过 D0143 根自引用拒绝 | 已修复 |
| 6-13 | 低 | O | `decode_notification` 的 known 警报名单漏 `error`(typed 失败静默降级 Unknown) | 已修复 |
| 6-14 | 低 | K | 15+3 个 admin 响应 DTO `object` 判别裸 String(pin 均封闭枚举,同族已开枚举化) | 已修复 |
| 6-15 | 低 | K | admin manifest(119+3)无 pinned spec 源 parity 测试(现有防线全部自引用) | 已修复 |
| 6-16 | 低 | M/G | multipart 下载 lane `http.route="/download"` 伪造 + 四 lane operation.id 非真实操作 ID | 已修复 |
| 6-17 | 低 | M | tracing 修复包:route_template 无条件分配、retry.count/retry.attempt 异名、RMCP 字段两套风格、emit_auth_refresh 单发 lane 文案失实、x509 deadline 漏 retry.count | 已修复 |
| 6-18 | 低 | M/P/K | tracing 泄露/覆盖测试扩展:admin lane、codex direct lane、SSE 流 no-leak、rmcp;tracing 立面决策落档 + rmcp/codex crate 文档 | 已修复 |
| 6-19 | 低 | 多域 | 测试缺口包:JSONL 行数/单行边界、FT reinforcement 超参边界、webhooks 配置面/fail-closed、input_items 已补(核对) | 已修复 |
| 6-20 | 低(文档) | 多域 | 文档包:AdminClientBuilder 旋钮默认值+预算语义、UsageQueryParams 字段文档、submit_jsonl_path 不校验声明+embeddings 50k、Uploads add_part Path 源、mint 泛型面警告、admin 代理注释误导修正 | 已修复 |
| 6-21 | 低 | L | `proxy(Option<reqwest::Proxy>)` 的 reqwest 不可经门面命名(rustdoc 未提依赖前提) | 已修复 |
| 6-22 | 低 | E/M | Realtime call-control Accept/空体立场未并入 D0223;WS/SSE 流 span 缺失未落档 | 已修复(决策) |
| 6-23 | 低 | O/M | 决策落档:codex TOCTOU、Windows 快照弱化、200MB 十进制、AdminInner Bearer 姿态、D0154/D0174/D0217 "schema-backed" 措辞修订 | 已修复(决策) |
| 6-24 | — | — | capture 测试助手两处重复(备注,不修) | 已拒绝 |
| 6-R1 | — | A | AdminListParams.limit 无校验 | 已拒绝(共享袋超集为 D0059 立场;补文档并入 6-20) |
| 6-R2 | — | K | 64 操作无便捷 facade | 已拒绝(泛型 request::<O>() 为完整面;立场并入 6-22 决策) |
| 6-R3 | — | M | span 名/字段与 OTel 完全对齐 | 已拒绝(6-17 只修异名与文案,命名空间保留 crate 风格并落档) |

## 关键详情(摘要)

- **6-01**:webhooks.rs:32,327-330 第 33 候选返回 InvalidSignatureHeader;node `webhookSignatureRequiresSigning` 只是切有界路径仍验证全部候选(`webhook-signature-amplification.test.ts` 明确断言 slot 33 与 1600 无效后有效均接受);python `any(compare_digest)` 无上限;8KiB 头上限已把候选约束 ~180 个。修复:去除 32 候选硬拒,保留 8KiB 头界内逐候选常量时间评估(与 node 有界路径对齐);更新 D0205 系测试。
- **6-02**:pin `v2/ThreadStartParams` 15 可选属性 Rust 建模 10,缺 approvalsReviewer/config/threadSource/serviceTier/sessionStartSource;`v2/TurnStartParams` 缺 sandboxPolicy/approvalPolicy(approvalsReviewer/serviceTier;turn 级覆盖整体不可用)。修复:补齐字段(approvalsReviewer 开枚举、sandboxPolicy 完整 tagged union 建模或 Value 逃生门、config Map、其余 String/枚举)+ 补 extra flatten;补序列化测试。
- **6-03**:client.rs:618-639 写超时只移除 pending 不 teardown;tokio write_all 非取消安全可留半帧。修复:写完成标志(AtomicBool);超时时未置位则 terminate(fail-stop);补回归测试。
- **6-04**:admin.rs:330-348(AdminNextPage)、3143-3154(UsageResponse)、fine_tuning.rs:2080(及 1943/1974 同形)。修复:统一空串过滤(None);next_cursor_with 镜像视情况;补测试。
- **6-05**:beta_responses.rs:392-405 `BetaPromptCachedInputContent::new(impl Into<InputContent>)` 可构造 ComputerScreenshot;pin beta 消息 content 全部 3 分支。修复:收窄构造面(移除 Into<InputContent> 泛型,仅 From<InputText|InputImage|InputFile>)+ validate 拒绝;decode 维持 4 分支(D0142 松桥);补测试。
- **6-06**:files.rs:977-1002 仅 path 脱敏,file_name/media_type 明文;client OneShot 侧已 `[REDACTED]` 且有测试。修复:对齐脱敏+测试。
- **6-07**:codex protocol.rs 27 处 pub extra derive Debug。修复:手写 redacted Debug(仅 len)或换 ExtraFields;泄露测试。
- **6-11**:chat.rs:2133-2159 policy 裸 Map;responses.rs:5414-5506 已定型。修复:复用 responses::ModerationPolicy(保留 with_policy 逃生门);补 wire 测试。
- **6-16**:multipart.rs:745 route 硬编码;:500/598/650 lane 常量 id。修复:download 传 path 用 route_template;lane 增加 operation_id 参数(调用方提供真实操作 ID)。
- **6-18**:tracing 决策条目:依赖无条件/无 feature 门/无公共钩子、WARN 级事件 vs python INFO/node 无、字段白名单与永不记录清单、span 命名清单;rmcp/codex crate 文档补 span 面;泄露测试扩 admin/codex direct/SSE 流。

## 决策落档类(6-22/6-23)

1. Realtime call-control `Accept: application/json`(Empty 族)与 reject_default 无 body 立场并入 D0223 系。
2. WS 连接与 SSE 流消费阶段无 span(span 仅覆盖握手;内容永不入 span)——落档立场。
3. Codex spawn hash→TOCTOU(单机威胁模型下接受)、Windows 快照弱化(len+mtime)、batch 200MB 十进制 fail-closed、AdminInner 持明文 Bearer(基线同严于基线)。
4. D0154/D0174/D0217 的 "schema-backed lower bound" 措辞修订为 "prose-backed lower bound"(下界 1 来自描述文字,非 schema minimum)。
5. admin 便捷 facade 为样品层、泛型 `request::<O>()` 为完整面(D0151/D0164 延伸)。

## 结论

Admin 全量深查:119+3 操作三向零偏差(独立复验),DTO 字段面高度一致;本轮发现集中于游标家族遗漏、tracing 新维度的工程缺口、codex 发送面缺口、脱敏不一致与测试防线,共 23 项修复 + 4 项拒绝/备注;第 5 轮修复回归 16/16 通过。

## 修复记录(2026-08-31 执行完毕)

- 23/23 项闭环:6-01~6-21 代码/文档修复(约 45 个新测试函数),6-22/6-23 立场以 D0231/D0232 落档。决策条目 D0225–D0232(8 条)已追加 `spec/contracts/decisions.md`(账本累计 232 条)。
- 修复分组:组1 admin(6-04/14/15/20)、组2 webhooks(6-01/19)、组3 codex(6-02/03/07/13+W3C trace)、组4 beta/chat types(6-05/11)、组5 multipart/files/media(6-06/16/20)、组6 containers/realtime/structured(6-08/09/12)、组7 tracing 包(6-17/18/21)、组8 batches/FT 测试(6-19/20)。
- 主代理收尾:6-10 三个 WS `close_reason()` 文档修正(coded close 可带空 reason);realtime create_call 单发 lane 改用 `emit_auth_refresh_no_retry`;组5 的 voices/content_provenance 两处最小越界调用点适配复核认可。
- 组间分歧处理:6-05 的约束错误落在 beta 侧新 envelope `BetaResponseInputConstraintError`(responses.rs 归属限制),行为正确、可后续塌缩。
- 全部门禁通过:fmt、clippy(-D warnings 零错)、全量测试 **954 passed / 0 failed**、no-default-features、xtask check、cargo deny。
- 遗留:无新待定项;tracing spans 对 WS 连接生命周期仍无覆盖(D0231 落档为立场)。
