# 问题12.md — 第 12 轮复扫(R2 镜头:serde 边界第二遍)

- 日期:2026-09-01
- 轮次:12 / 20(R2 serde 边界复扫;6/8 领域零发现)
- 基线:同前;第 11 轮修复回归全部通过

## 汇总表

| 编号 | 严重度 | 领域 | 问题 | 状态 |
|---|---|---|---|---|
| 12-01 | 中 | T/R | platform 通道查询数组编码裸重复键 vs pin 括号形态(python client 级 Querystring(brackets) 覆写 + pin curl 示例 `include[]` 双重证据;D0238 已修 admin,platform 留待决策——现应修复) | 已修复(D0251) |
| 12-02 | 低 | D | `ChatFunctionTool::for_type` 不校验名称(11-05 分径修复漏掉 chat 入口;chat pin 上限 64) | 已修复(D0252;审查项13追加 request 级校验) |
| 12-03 | 低 | R | beta 解码侧 DTO 的 ExtraFields 缺失家族(GA/beta 镜像不一致:BetaPromptCacheOptions(Param)/BetaAgentMessage/BetaMultiAgentCall(Output)/inject 事件/BetaCompactResponseRequest;未知键 round-trip 有损) | 已修复(D0253) |
| 12-04 | 低 | H | JSONL writer 不校验 `custom_id` 非空(decode-through 旁路:解码空回显行→重写可产出空 custom_id 输入行;与 D0241 NonObjectBody 同构) | 已修复(D0254) |
| 12-05 | 信息 | T | admin.rs 注释对 python 的事实陈述错误("client 级 Querystring 仍重复裸键"——实际为 brackets 覆写) | 已修复(随 D0251) |
| 12-06 | — | 多域 | 落档类:top_logprobs_null 的 pin-lag 仲裁(python/node 共识 + pin 资源侧自证)、Response.store 超集、realtime include 可空超集、beta 包装器 null 剥离冗余、codex 接收面 None→null 不对称、evals per_model_usage 宽松、safety webhook pattern | 已落档(D0255) |
| 12-R1 | — | C/L/杂项 | serde 复扫 | 零发现 |

## 修复分工

- 组1(transport+chat+batches):12-01、12-02、12-04、12-05
- 组2(beta types):12-03
- 主代理:12-06 落档(D0251)

## 结论

R2 复扫收敛持续:6/8 零发现;1 中(platform include 括号——第 7 轮决策债的清偿)+ 4 低;第 11 轮修复(根 $schema/variables 三态/名称分径/beta validate/wait_for_processing)回归全部在位。

## 修复记录(2026-09-01)

两个修复代理按文件所有权并行执行,全部完成:

| 编号 | 修复内容 | 落档 |
|---|---|---|
| 12-01 | `append_query` Array 分支改为 `name[]` 括号拼写(key 每字段构建一次);标量丢弃规则(D0145)与嵌套对象拒绝不变;doc 注释补 python `_client.py:519-520/1261-1262` 覆写、node `query.ts` brackets、pin `include[]=` curl 三重引证;受影响断言更新为 `include%5B%5D=`(url crate 百分号编码,与 Administration 通道 `project_ids%5B%5D` 先例一致) | D0251 |
| 12-02 | 新增 `MAX_CHAT_FUNCTION_TOOL_NAME_CHARS = 64`(pin 行 31670/21390 散文约束:非空、≤64、a-zA-Z0-9_-)+ `validate_chat_function_tool_name` 复用共享 charset 规则;`ChatFunctionTool::for_type` 构建前校验,镜像 D0247 Responses 形态。审查项 13 追加:request 级 `CreateChatCompletionConstraintError::FunctionName` 变体 + `validate` 遍历 `tools[].function.name` 与弃用 `functions[].name`(GA Responses 在 request 级同样校验,chat 原先漏掉手工构建路径) | D0252 |
| 12-03 | 九个 beta 解码 DTO 补 `#[serde(flatten)] extra` + `extra_fields()` 访问器(逐一对齐 GA 镜像);`BetaResponseInjectEvent` 按 GA 镜像(`ResponsesCreateEvent`)同样省略而跳过;`BetaResponseInjectError` 因 GA 嵌套错误载荷(`ResponsesWebSocketErrorDetails`)保留 extra 而纳入;刷新过期的 `BetaResponsesServerEvent` 枚举注释;编码输出不变 | D0253 |
| 12-04 | `write_line` 镜像解码侧语义(精确空串、不 trim)新增 `BatchJsonlError::EmptyCustomId { line }`,位于行数上界之后、去重检查之前,拒绝不毒化 writer | D0254 |
| 12-05 | `append_query_value` doc 更正:删除"两官方 SDK 不一致"的错误陈述,记录 python client 级 brackets 覆写与 node 一致;Administration 行为不变 | D0251(随附) |
| 12-06 | 落档:top_logprobs null 项按 SDK 共识仲裁、Response.store 超集、realtime include 可空超集、beta 包装器 null-strip、codex 接收面不对称、evals per_model_usage 宽松、safety webhook pattern、`BetaAgentMessageParam`/`BetaMultiAgentCall`/`BetaMultiAgentCallOutputParam` 留待下一 serde 镜头(其 GA 镜像不在本轮证据集) | D0255 |

新增测试:`chat_function_tool_for_type_enforces_the_64_char_name_pin`、`writer_rejects_empty_custom_ids_before_writing`、`writer_keeps_accepting_lines_after_an_empty_custom_id_rejection`、`beta_prompt_cache_dtos_retain_unknown_fields`、`beta_agent_and_multi_agent_items_retain_unknown_fields`、`beta_inject_server_events_retain_unknown_fields`、`beta_compact_request_retains_unknown_fields`,及 transport/client 断言更新。代理侧验证:fmt/clippy 干净,client 438 + types 505 测试全绿。
