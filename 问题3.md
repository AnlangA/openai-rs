# 问题3.md — 第 3 轮对比审计(镜头:枚举域与 ID/判别格式)

- 日期:2026-08-31
- 轮次:3 / 20(16 领域全扫 + 枚举域/判别值/ID 格式镜头)
- 基线版本:同前(pinned OpenAPI `690521b`、openai-python `b19c2161`、openai-node `eea2292a`、Codex 0.144.5)
- 第 2 轮修复回归抽查:16 域全部通过;发现 1 处不完整(GA compact service_tier,2-16 只修了 beta 侧,列为 3-01)
- 遗留闭环:2-D1(realtime keepalive 方案已评估,采纳 opt-in)、2-D2(判定 D0050 依据错位,折中修复)、2-D3(统一 admin 查询编码)

## 汇总表

| 编号 | 严重度 | 领域 | 问题 | 状态 |
|---|---|---|---|---|
| 3-01 | 中低 | A | GA compact `service_tier` 复用 7 值超集(pin 5 值;2-16 只修了 beta 侧) | 已修复 |
| 3-02 | 中低 | O | Codex ThreadStart/TurnStart 的 approval_policy/sandbox/personality/summary 为 pin 闭合枚举却用裸 String;AskForApproval 的 granular 对象变体不可表达 | 已修复 |
| 3-03 | 中低 | M/K | `AdminClient` 通道完全没有重试面(119 操作含只读 GET,与平台通道不一致且无决策) | 已修复 |
| 3-04 | 低 | A | easy-form 输入消息 role 构造域为 8 值(pin 请求域 4 值) | 已修复 |
| 3-05 | 低 | A | `computer_screenshot` 进入 param 侧内容联合(easy-form/函数输出),pin 请求侧 3 分支 | 已修复 |
| 3-06 | 低 | A | `ResponseItemStatus` 8 值并集作所有 item 的请求可构造域(pin 逐 item 小域) | 已修复 |
| 3-07 | 低 | E | legacy realtime 数值约束解码期硬校验(speed/temperature/max_output_tokens/secret_expiration),违反 opt-in 策略 | 已修复 |
| 3-08 | 低 | E | legacy `/realtime/sessions` 响应形态 pin 疑为请求/响应互换 artifact,两官方 SDK 沿用旧形态;`client_secret` 必填是唯一硬失败点 | 已修复 |
| 3-09 | 低 | G | edit 两侧 `quality` 复用 6 值生成枚举(multipart edit pin 5 值无 hd;JSON edit pin 4 值) | 已修复 |
| 3-10 | 低 | G | JSON image-edit `size` 复用 8 值开放枚举,pin 该处封闭 4 值 | 已修复 |
| 3-11 | 低 | I | FT reinforcement grader 联合超集(顶层多 label_model;嵌套 multi 可递归;evals 侧已有正确拆分) | 已修复 |
| 3-12 | 低 | I | VS 列表 limit 1..=100 上限为散文发明值(D0154 同型未同步) | 已修复 |
| 3-13 | 低 | J | skills `object` 共享双值枚举跨三宿主(本 crate 惯例是分型) | 已修复 |
| 3-14 | 低 | K | `AssignmentObject` 含 4 个无任何基线依据的幻影变体 | 已修复 |
| 3-15 | 低 | K | usage 侧 `group_by` 未类型化(10 值并集枚举缺失) | 已修复 |
| 3-16 | 低 | K | `AdminListObject` 含 `page` 变体(响应侧无害超集,顺手收窄) | 已修复 |
| 3-17 | 低 | K/M | admin 查询编码器对显式 null/空串发 `key=`(2-D3,统一到 D0145 规则) | 已修复 |
| 3-18 | 低 | L | `open_string_enum!` 生成的 `Borrow<str>` 与派生 Hash/Eq/Ord 违反一致性契约(潜在集合键陷阱,当前零调用点) | 已修复 |
| 3-19 | 低 | L | `$ref` 兄弟键内联输出可为指数级(DAG 扇出,无大小预算) | 已修复 |
| 3-20 | 低 | M | realtime `create_call` 无显式 RetryClass(分类学不闭合) | 已修复 |
| 3-21 | 低 | N | webhook 签名候选上限分支未被测试实际覆盖(经旁路通过) | 已修复 |
| 3-22 | 低 | O | Codex UserInput image detail 裸 String(pin 4 值闭合) | 已修复 |
| 3-23 | 低 | O | Codex 接收侧 5 处 open String vs pin 闭合 enum(Turn.status/CancelLogin status/plan_type×2/rate_limit_reached_type/auth_mode) | 已修复 |
| 3-24 | 低 | P | RMCP 工具名清洗阈值 64 与 pin(128)及 types 侧自身校验不一致 | 已修复 |
| 3-25 | 低 | P | 门面缺 `AdminOperation`/`AdminQuery` 等 8 项与 content-provenance 3 个 DTO 名 | 已修复 |
| 3-26 | 低 | P | README Responses 样例与可执行 example 分叉,"same code" 声明失真 | 已修复 |
| 3-27 | 低 | P | `LosslessEnvelope` 丢弃 rmcp `resultType`(SEP-2322)与 `_meta` | 已修复 |
| 3-28 | 低 | E | 2-D1 闭环:`RealtimeWebSocketConfig` 增加 keepalive 选项(opt-in,默认关闭) | 已修复 |
| 3-29 | 中低 | F | 2-D2 闭环:`BetaPromptCachedInputMessage` 删 `id_null()`/`status_null()`(无 pin 依据),保留非空 setter+文档,修正 D0050 依据错位 | 已修复 |
| 3-30 | — | 多域 | 决策记录类:3-C1(SSE 三项强校验)、3-E1(legacy 形态跟随基线)、3-J1(MultiGrader 数组官方示例兼容)、3-M4(Accept 精化)、3-H1(Uploads bytes 预检) | 已修复(决策条目) |
| 3-R1 | — | A | `ModelId` 常量仅 4 个(其余 98 值无常量) | 已拒绝(D0130 常量为便利,非缺陷) |
| 3-R2 | — | E | GA `include` 开放 `Vec<String>`(pin 单值枚举) | 已拒绝(开放解码无损,仓策) |
| 3-R3 | — | J | `ConversationMessage.status` 复用 8 值 ResponseItemStatus(响应侧命名超集) | 已拒绝(开放枚举语义等价,无发送面) |

## 关键问题详情(摘要)

- **3-01**:responses.rs:7120 GA `CompactResponseRequest.service_tier` 复用 7 值 `ServiceTier`;pin `CompactResponseMethodPublicBody.service_tier` → `ServiceTierEnum` 5 值(auto/default/fast/flex/priority),node 同。修复:与 beta 侧 `BetaCompactServiceTier` 同构的 GA 5 值开枚举(或抽公共类型两侧共用),create 侧不动。
- **3-02**:protocol.rs:270-274/401-403;pin `v2/AskForApproval`(3 字符串值+granular 对象)、`v2/SandboxMode`(3 值)、`v2/Personality`(3 值)、`v2/ReasoningSummary`(auto/concise/detailed/none)。修复:开枚举(D0115 模式)+ AskForApproval 二元 union(枚举+granular 结构体,mcp_elicitations/rules/sandbox_approval 必填)。
- **3-03**:client/admin.rs:2173-2210 `AdminClient::send` 单发无重试。修复:接入 `RetryPolicy`(GET/DELETE→Safe,POST→Replayable 受 `retry_replayable_mutations` 门控,复用 transport 语义);builder 暴露 `with_retry_policy`;补 429→200 重试测试。
- **3-06**:responses.rs:37-46;pin 逐 item status 域(MessageStatus 3 值/MCPToolCallStatus 含 calling+failed/WebSearch 含 searching/FileSearch 5 值/ImageGen 含 generating/CodeInterpreter 含 interpreting)。修复:逐宿主小开枚举+From 互转,构造 setter 收窄(参照 D0115/D0157 先例)。
- **3-17**:client/admin.rs:2383-2406;`Value::Null`/空串改跳过(对齐 D0145/transport/python);更新 3278 行测试断言;admin 唯一 Nullable 查询字段 `after` 显式 null 等同省略。
- **3-18**:scalar.rs:39-48/110-129;移除宏生成 `impl Borrow<str>`(保留 `AsRef<str>`)——derive 枚举 Hash/Eq/Ord 混入判别式,与 Borrow 契约冲突;全仓无枚举键集合,零调用点破坏。
- **3-19**:structured.rs:559-581;内联加展开节点数预算(如 100_000)超限报错(新变体或复用 UnsupportedKeyword 风格),防 DAG 扇出指数爆炸。
- **3-27**:rmcp result.rs:29-45;envelope 增 `result_type`(Option,缺省 skip,缺键语义=complete)与 `meta`(`_meta`);`from_rmcp` 复制;补 round-trip 测试。
- **3-28**:2-D1 采纳方案:`RealtimeWebSocketConfig.keepalive: Option<RealtimeKeepalive{ping_interval, pong_timeout}>`(默认 None=关闭);recv/send 以 select! 轮转 ticker;判据"超时窗内无任何入站帧"(非逐 Pong 匹配);超时报 `WebSocketProtocol` 不自动重连;python 默认 20/20 作文档参照。
- **3-29**:2-D2 判定(三方证据):`id_null`/`status_null` 无任何 pin 分支依据,删;`id()`/`status()` 非 null 保留+rustdoc 注明"重放兼容超集";`agent`/`phase` setter 有依据保留;追加决策修正 D0050 Sources 归类;更新 `beta_item_official_nulls_match_openapi`。

## 决策记录类(3-30,主代理落档)

1. **SSE 三项强校验**(Content-Type 门/`event:` 行存在且==data.type):python/node 均不校验,官方夹具均满足;记录为有意的防错路由硬化(beta 复制同口径)。
2. **legacy `/realtime/sessions` 响应形态**:pin 的请求/响应 schema 疑互换(响应含新嵌套形态无 client_secret,请求反而 required client_secret);python(beta)/node 沿用旧扁平形态与 Rust 一致;跟随两官方 SDK 基线,防御性放宽 `client_secret` 为 Omittable(3-08 代码侧)。
3. **MultiGrader `graders` 数组形态**:pin schema 为单对象,官方 x-oaiMeta 示例用数组;保留 One|Many 双形态解码+显式 `many()` 构造(用户显式选择),落档。
4. **Accept 头精化**(SSE `text/event-stream`、转写按格式 `text/plain`/`x-subrip`/`text/vtt`、void `application/json`):python 恒发 `application/json`,服务端不据 Accept 分支;记录为有意的 HTTP 语义精化。
5. **Uploads `bytes<0` 本地预检**:pin 无 minimum,python/node 照发;与 PathSegment 守卫同类的防御性检查,仅拦截语义不可能值,落档边界说明。
6. **realtime `create_call` RetryClass::Never**(3-20 代码侧):创建通话有副作用(可能双开通话),与 accept/reject/hangup/refer 同保守分类,显式标注+落档(python 默认重试所有请求为对立基线,记录分歧)。

## 结论

枚举/判别镜头下:B/D/H 零发现;其余领域 25 项代码修复(0 高、3 中低、22 低)+ 6 项决策记录;2-D1/2-D2/2-D3 三项遗留全部闭环;第 2 轮修复回归 16/16 通过(1 处不完整列 3-01)。3 项新拒绝。

## 修复记录(2026-08-31 执行完毕)

- 30/30 项闭环:3-01~3-29 代码修复,3-30 六项立场以 D0187 落档;2-D1(D0171 keepalive)、2-D2(D0188:删 id_null/status_null、保留非空 setter+重放超集文档、修正 D0050 依据错位)、2-D3(D0180)全部闭环。决策条目 D0165–D0188(24 条)已追加 `spec/contracts/decisions.md`。
- 修复分组:组1 responses(3-01/04/05/06)、组2 legacy realtime(3-07/08)、组3 realtime keepalive(3-28/20)、组4 media(3-09/10)、组5 FT/VS(3-11/12)、组6 skills/门面/README(3-13/25/26)、组7 admin(3-03/14/15/16/17)、组8 scalar/structured(3-18/19)、组9 codex(3-02/22/23)、组10 rmcp/webhook(3-24/27/21)。
- 主代理收尾:3-06 三处宽域签名翻转(StoredInputMessage::status/OutputMessage::new/FunctionCall::new 收窄为逐宿主枚举)及 5 处越权调用点适配(conversations 生产转换经 from_raw 重放、structured/rmcp 测试);kernel.rs `tagged_union_reject_known!` 加 `#[cfg(feature="legacy-evals")]`(消除依赖 crate 特性统一构建下的既有 unused_macros);`RealtimeKeepalive` 补 client 根与门面导出。
- Codex 组偏差说明:任务书笔误"granular camelCase",pin 实为 snake_case 键——按唯一基线 pin 保留 snake_case 并测试锁定。
- 全部门禁通过:fmt、clippy(-D warnings 零错)、全量测试 **777 passed / 0 failed**、no-default-features、xtask check、cargo deny。
- 无新待定项;遗留仅 large_enum_variant Box 化重构(工程债,非 wire)。
- 审查整改:第3轮审查通过(3 项零风险观察已处理):`tls_backend` 恢复 `const fn`(与平台侧对称);12 条决策补齐 Reason 字段(1 条原本已有);计数口径已按审查修正(测试字面量适配实为 14 处、调用点按文件 6 处)。
