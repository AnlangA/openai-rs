# 问题8.md — 第 8 轮对比审计(镜头:测试与 fixture 缺口)

- 日期:2026-08-31
- 轮次:8 / 20(8 领域专项:各域测试覆盖/fixture 引用/账本 Tests 名实/决策防线)
- 基线:第 1-7 轮已修复 218 项(decisions.md D0113–D0244),全量 1006+ 测试
- 第 7 轮修复回归:全部通过(7-01/7-04/7-05/7-06/7-08/7-09/7-17/7-18/7-19/7-20/7-22/7-25 抽查全绿)

## 汇总表

| 编号 | 严重度 | 领域 | 问题 | 状态 |
|---|---|---|---|---|
| 8-01 | 中 | G | testdata 六个 fixture 五个死文件;D0007/OVR-0006 声称的"空列表 required-ids"测试实际不存在(任何形态) | 已修复 |
| 8-02 | 中 | D | Chat 请求体 35 字段无全体 round-trip,约 17 字段全仓零 wire 覆盖(builder 从未被调用) | 已修复 |
| 8-03 | 中 | D | 流式 final usage chunk(choices=[]+usage)零覆盖(Chat/legacy 双线);请求侧 include_usage 有测但响应形态未测 | 已修复 |
| 8-04 | 中低 | R | GA `ResponseStreamEvent` 57 tag 中约 32 个零正向解码测试(五组 hosted-tool 生命周期三态+delta 族);`Annotation` 4 分支仅 1 测 | 已修复 |
| 8-05 | 中低 | C | `collect_with`(resume 消费路径)零调用零测试;GA SSE 两个 StreamProtocol 守卫+UnexpectedContentType 握手失败零测试 | 已修复 |
| 8-06 | 中低 | F | kernel `object_discriminator` 三对抗分支(非对象/缺 type/type 非字符串)DTO+kernel 层零测试 | 已修复 |
| 8-07 | 中低 | K | admin usage/costs 全族零 loopback;`UsageResult` 11 分支中 7 个零 fixture 且无 pin 派生 parity | 已修复 |
| 8-08 | 中低 | K | audit 55 payload 字段级 fixture 仅 4/55;`{}` 探针对字段拼写/类型错误不设防(Err 分支不可达) | 已修复 |
| 8-09 | 中低 | B | beta 58 事件包装器约 5% 覆盖;`item` 分支(output_item.added/done)零测试;inject.failed 零测试 | 已修复 |
| 8-10 | 中低 | T | 重试矩阵"x-should-retry"覆盖与 408/409 状态零直测;传输错误重试 lane(is_connect/is_timeout)与 DeadlineExceeded 全仓零断言 | 已修复 |
| 8-11 | 中低 | T | 三 WS 客户端共通缺口:握手超时分支、401 凭据刷新重试(REST 同构)、超限 send"本地校验保活"半边;max_message_bytes 出入站三分支×三客户端零覆盖 | 已修复 |
| 8-12 | 中低 | O | codex app_server 四类公开错误路径零测试(PendingCapacityTimeout/EventQueueFull/OrphanResponse/InvalidMessage 四子分支);Notification 10 类型化分支 5 个零测试 | 已修复 |
| 8-13 | 中低 | 全 | fuzz 3 目标零执行路径不进任何编译门禁(exclude 且无文档);trybuild 只钉 12 特性边界中 2 个 | 已修复 |
| 8-14 | 低 | D | 流式对抗:三 lane 坏 JSON 帧零测试;Chat 已填充 logprobs 零 decode;D0016 的 8 族 validate 限制 6 族无错误断言 | 已修复 |
| 8-15 | 低 | G | media 客户端"响应形态×格式"矩阵 2/11;multipart 字段编码断言 10/24,嵌套对象 bracket 零 wire 断言;死模式分支 | 已修复 |
| 8-16 | 低 | D | Chat tool/choice/response_format 变体覆盖空洞(Custom/Mode/Allowed/json_schema/Refusal 零测试) | 已修复 |
| 8-17 | 低 | F | beta 资源侧 moderation 零测试(GA/beta 不对称);D0135 计数陈旧 | 已修复 |
| 8-18 | 低 | H | beta background `poll` 五消费者中唯一零测试;VS files/file_batches poll 零测试 | 已修复 |
| 8-19 | 低 | E | `SessionUpdate` 嵌套 validate 委托零测试;chatkit list_item_pages 零测试;beta WS send_create 零 wire | 已修复 |
| 8-20 | 低 | J | 8 个手写分页 glue 中 4 个零测试(skill versions/eval runs/eval output items/voice consents) | 已修复 |
| 8-21 | 低 | J | spend_alerts(10)/spend_limit(6)无 facade 无 wire 测试,缺口未落档 | 已修复 |
| 8-22 | 低 | O | direct OAuth 成功链路/401 刷新重试零 e2e;jwt 15 分支仅 4 测;keyring 入参校验未测;validate_body 拒绝分支未测 | 已修复 |
| 8-23 | 低 | T | `SseStreamDecoder::close()` 公开 API 零调用零测试;SSE 限值两防御边界(悬挂 CR/BOM 前缀)无测试 | 已修复 |
| 8-24 | 低 | P | rmcp SchemaPolicy::Preserve 零测试;退化映射前缀回退零测试;bridge with_result_encoding 未测 | 已修复 |
| 8-25 | 低 | P | assert_wire 仅 21/1002 pub 类型;doc-test 全仓仅 2;contract-tests wip/ 残留 3 文件 | 已修复 |
| 8-26 | 低(账本) | 多 | 账本 Tests 名实不符 2 处(D0187 测试名陈旧、D0016 覆盖广度不符);manual-overrides.toml tests 标签与实际测试名不一致 | 已修复 |
| 8-27 | 低 | H | 7-17 回归:add_part 预检与"prepare-time freeze"语义不精确,Path 源存 TOCTOU 窗口(文档/测试名与内容不符) | 已修复(文档) |
| 8-R1 | — | R | `CustomToolFormat::Text` 默认分支 | 已并入 8-04(顺手) |

## 关键详情(摘要)

- **8-01(中)**:唯一被引用 fixture 是 `downloadFile/response.bin`(multipart.rs:1628);`listFiles/empty.json`、`mcp-approval/*.json` 等零代码引用且账面声称的测试不存在;`"data": []` 零命中,"空列表仍要求 cursor ids"的 OVR-0006 契约防线完全落空。修复:fixture 接入 include_str 断言或删除死文件并改账本引用实际测试名;至少补空列表 required-ids 内联测试。
- **8-02/8-03(中)**:Chat 17 字段(builder with_response_format/with_max_completion_tokens 从未调用)与 final usage chunk 双线零覆盖。修复:kitchen-sink round-trip+键集合断言(镜像 legacy 的 inventory 测试)+usage chunk 双线测试。
- **8-04(中低)**:32 个流事件 tag 零正向测试(required_stream_event 宏产物 13 个仅剩编译期断言);Annotation 4 分支仅 url_citation。修复:参数化 fixtures 循环(每未测 tag 一条最小 payload decode→re-encode 等值),五组 hosted-tool 三态+audio/refusal/reasoning delta 族优先。
- **8-06(中低)**:`{"type":123}`/缺 type/非对象三个对抗分支在 kernel 与 DTO 层均零测试——Unknown 降级前提无回归钉住。修复:kernel 测试模块直接钉住三分支+三个主要 union 各一条。
- **8-07/8-08(中低)**:usage 11 result 分支 7 个零 fixture 且无 pin 派生 parity(realtime/audit 已建立该标准);audit `{}` 探针恒 Ok,字段拼写错误不设防。修复:镜像 realtime/audit 的 pin 派生(operations/discriminators.json 或 openapi include_str);audit payload 字段名集合断言。
- **8-09(中低)**:beta 包装器 item 分支在任何测试中从未被填充;58 tag 约 5% 覆盖。修复:58 tag 穷举 fixture 循环(镜像 realtime)+inject.failed。
- **8-10(中低)**:三份 should_retry 副本无直测;"x-should-retry: true 强制 400 重试"/"false 抑制 5xx"/408/409 零 e2e;is_connect/is_timeout 重试与 DeadlineExceeded 全仓零断言。修复:真值表单测(或抽共享函数)+4 条 e2e。
- **8-11(中低)**:握手超时、401 刷新(三 WS+REST 同构)、超限 send 保活、max_message_bytes 出入站全零。修复:各面补齐(挂起握手 server+50ms 超时、先拒后放+可刷新 AuthProvider、小限额出入站)。
- **8-12(中低)**:PendingCapacityTimeout/EventQueueFull/OrphanResponse/InvalidMessage 四子分支零测试;5 个通知分支零测试(AgentMessageDelta 是增量文本唯一通道)。修复:fake-child 脚本补四错误路径+5 通知 decode。
- **8-13(中低)**:fuzz 零执行零门禁零文档;trybuild 缺 webhooks/chatkit/legacy-completions/custom-voice 负例。修复:门禁加 `cargo check -p openai-rs-fuzz`(若 fuzz 是独立 crate 无法 -p,则 cargo check --manifest-path)+development.md 手动流程;补 4 个 trybuild 负例或落档。
- **8-26(账本)**:D0187 测试名、D0016 广度、manual-overrides.toml 四处 tests 标签;修正或注记。

## 结论

测试缺口镜头:全量 1006+ 测试密度高、决策账本 Tests 名实 95%+ 相符、第 7 轮修复回归全部在位;缺口集中于:fixture 死文件与落空契约(8-01)、宏产物/包装器的正向穷举(8-04/8-09)、重试矩阵未测象限(8-10/8-11)、pin 派生 parity 标准未推广(8-07/8-08)、fuzz/trybuild 门禁盲区(8-13)。合计 27 项(3 中、10 中低、14 低);无功能缺陷,全部为测试防线/文档/账本卫生。

## 修复记录(2026-08-31 执行完毕)

- 27/27 项闭环(8-R1 并入 8-04 顺手)。全部为测试/fixture/账本/文档修复,无 wire 行为变更;新增约 95 个测试函数,全量 **1101 passed / 0 failed**。
- 修复分组:组1 fixture+账本(8-01/26/27/25wip)、组2 Chat/Media(8-02/03/14/15/16,代理汇报超时但改动完整落盘并经主代理逐项核验:kitchen-sink/final usage chunk/坏 JSON 三 lane/logprobs/6 族 validate/格式矩阵/multipart 嵌套/变体族)、组3 Responses 流+kernel(8-04/05/06/17,超时落盘核验:32+ 流事件正向 fixtures/Annotation 4 分支/resume+collect_with 端到端/两 lane content-type 门/畸形帧/object_discriminator 三形态/beta moderation)、组4 Admin/FH/FT(8-07/08/18/20/21)、组5 传输/WS(8-10/11/23/13,超时落盘核验:retry 真值表+4 e2e/握手超时+401 刷新+超限 send/SSE close+防御边界/fuzz 稳定门禁+4 trybuild 负例)、组6 codex/rmcp(8-12/22/24/19)。
- 三个代理汇报超时但改动均完整落盘;主代理对每组的关键测试函数逐一 grep 核验后确认。
- 账本:D0245 落档;D0187/D0016/D0135 与 manual-overrides.toml 四处 tests 标签已同步实名(组1)。
- 全部门禁通过:fmt、clippy(-D warnings 零错;默认/最小特性零警告)、全量 1101 测试、no-default-features、xtask check、cargo deny;fuzz 纳入稳定门禁(development.md 更新)。
- 遗留:无新待定项;assert_wire 扩展(8-25)仅完成 wip 清理,薄抽样立场可后续落档。
