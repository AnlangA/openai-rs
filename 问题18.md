# 问题18.md — 第 18 轮(R9 镜头:回归验证第二遍)

- 日期:2026-09-02
- 轮次:18 / 20(R9 回归验证;16 验证代理 = 6 决策切片 + 2 时序重复 + 8 专项复扫)

## 验证矩阵与结果

| 项 | 方法 | 结果 |
|---|---|---|
| 决策 D0001-D0284 | 6 个切片逐条对照实现 | **零回归**(284/284;D0039 一处 ledger 测试名漂移已更正) |
| ledger Tests 完整性 | 17-L 普查(第 17 轮) | 431/431 命中且可运行 |
| 时序敏感测试 | client 12 个 ×15、codex/rmcp 17 个 ×10(350 次) | 1 flake:keepalive 5s 上限负载敏感(6.7s 迟到)——**已放宽为 30s 防挂起断言**;其余 0 失败 |
| 全量套件 ×2-3 | workspace --all-features | 1252 通过 / 0 失败,逐次一致 |
| 13+1 门禁 | 独立重跑 | **14/14 绿**(fmt/clippy×3/全测/feature×4/MSRV×2/xtask/deny/rustdoc) |
| 特性矩阵 | 14 个窄组合 cargo check | 1 告警:legacy-realtime-without-realtime 下 `RetryClass::Never` dead-code——**已 cfg_attr 修复**;其余全净 |
| drift 标记六类 | TODO/FIXME/再生成/夹具/unwrap/状态 | 全零残留;SOURCES pin 哈希一致;D0xxx 引用 ≤D0284 无悬空 |
| 机器 parity | admin 投影/查询/审计/union/codex/rmcp/xtask/contract | 全绿 |
| 安全/追踪 | span 全景、redaction 36 测、15-17 轮 diff 打印/密钥 | 零发现(无新 span、无生产打印、无密钥) |
| ledger 文本 | 编号/模板/交叉引用/修订链 | 全部完整,修订链 7 条逐一确认 |
| 文档/示例 | 7 示例编译、分页节 vs 代码、rustdoc | 全一致 |
| 跨 SDK 独立抽检 | 20 项高频线上行为 vs pin+双 SDK | **20/20 ✓**,零 ✗ |

## 本轮修复(3 项)

1. `RetryClass::Never` 窄组合 dead-code 告警 → `cfg_attr(not(realtime), allow(dead_code))` + 注释(operation.rs)
2. keepalive 测试 5s 上限 → 30s 防挂起断言 + 负载敏感注释(realtime.rs)
3. D0039 ledger 测试名漂移更正(`grader_sampling_params_...`)

## 结论

R9:全量回归验证通过——284 项决策、1252 个测试、14 项门禁、350 次时序重复、20 项跨 SDK 抽检全部绿;发现并修复 2 处测试健壮性/1 处文档漂移,零功能回归。为第 19-20 轮终态收敛就绪。
