# 问题10.md — 第 10 轮收尾审计(前半程终审)

- 日期:2026-08-31
- 轮次:10 / 20(前半程收尾:遗留闭环+最后横向深挖)
- 第 1-9 轮回归:全部通过(第 9 轮独立验证)
- 总体结论:前半程 245 项修复后代码面高度一致,**本轮零中高 wire 缺陷**;12 项低/信息级收尾项

## 汇总表

| 编号 | 严重度 | 领域 | 问题 | 状态 |
|---|---|---|---|---|
| 10-01 | 中低 | O | codex direct `DirectError::Json` 保留 serde 原始错误文案(payload 字面量可入错误链;D0206 已中立化 app-server 侧,direct 未同步且无落档) | 已修复 |
| 10-02 | 低 | P | 第 8 轮新增 4 个 spend facade 类型未透传门面(2-34 同族复发) | 已修复 |
| 10-03 | 低 | R | 两份 `validate_stream_id` 拒绝分支零测试;去重成本极低(收尾轮顺手) | 已修复 |
| 10-04 | 低 | R | `BetaResponse` 缺 GA 对等 `output_text()` 便利访问器(beta-only AgentMessage 文本不可达) | 已修复 |
| 10-05 | 低 | J | evals 三个 SamplingParams 的 `reasoning_effort` 非 Nullable(pin anyOf-null;FT 侧正确;decode 遇服务端回显 null 会击穿 EvalRun) | 已修复 |
| 10-06 | 低 | D | `CHAT_CREATE_NULLABLE_FIELDS` 漏 `prediction`(23/24;测试名声称全集) | 已修复 |
| 10-07 | 低 | G | D0163 的"multipart 空串照发"决胜立场无测试防线 | 已修复 |
| 10-08 | 低 | L | structured 的 `RootMustBeObject` 与"$ref 目标非对象"两分支零测试 | 已修复 |
| 10-09 | 低(账本) | 多 | D0236 "eleven direct tests" 补点名;assert_wire 薄抽样立场落档;PromptCache param 三实现 extra 不对称落档;filename 默认值差异落档;direct SSE 无 data 行数上限落档;direct 流式响应头无预算落档;AdminClient 无 with_request_timeout 落档;双 reqwest 栈落档;fuzz 不继承 forbid 落档;beta item_reference agent 构造面落档;transcription_session.updated 未类型化落档;spend loopback 14/16 落档 | 已修复(决策条目 D0246) |
| 10-10 | 低 | P | rmcp HTTP 传输遵循 env 代理(reqwest 0.13 默认 system proxy),与全仓"env 代理永不读取"姿态背离且未披露 | 已修复(文档+落档 D0246) |
| 10-R1 | — | O | codex 通知面 10/68 类型化立场 | 已确认自洽(与请求面同构;pin-derived parity 可选加固) |
| 10-R2 | — | R | BetaAgent 严格叶子 | 已确认维持(pin 双侧单字段) |
| 10-R3 | — | M | rmcp 与最新 MCP 规范 | 已确认零漂移(锁定 rmcp 3.1.4 即 2026-07-28 规范实现) |

## 修复分工

- 主代理:10-09 账本条目(D0246)
- 组1(codex+rmcp):10-01、10-10、10-03(去重)
- 组2(types):10-04、10-05、10-06、10-07、10-08、10-02(门面)

## 结论

前半程收尾:12 项(1 中低 + 8 低 + 1 账本包 + 1 文档);零中高;三客户端姿态矩阵/转换矩阵/安全红线/依赖面全部横向终审通过;rmcp 与最新 MCP 规范零漂移。

## 修复记录(2026-08-31 执行完毕)

- 12/12 项闭环:10-01~10-08、10-10 代码/文档修复(约 18 个新测试函数),10-09 十四项立场以 D0246 落档(含 D0236 十一个测试名补点)。决策 D0246 已追加(账本累计 246 条)。
- 修复分组:组1 codex/rmcp(10-01 Json 中立化/10-03 校验器去重+拒绝测试/10-10 rmcp 代理披露)、组2 types/facade(10-04 output_text/10-05 reasoning_effort 三态/10-06 prediction/10-07 空串钉/10-08 畸形根/10-02 spend 门面)。
- 组2 一处签名偏差:10-04 采用与 GA 完全一致的 `String` 返回(聚合多 part 无法返回借用切片)——主代理认可。
- 全部门禁通过:fmt、clippy(-D warnings 零错)、全量测试 **1114 passed / 0 failed**、no-default-features、xtask check、cargo deny;仅 beta-responses 特性组合编译验证通过。
- 前半程(1-10 轮)总账:258 项修复、D0113–D0246 决策、1101→1119 测试、零未闭环遗留(全部为已落档立场)。
