# 问题2.md — 第 2 轮对比审计(镜头:serde 边界)

- 日期:2026-08-31
- 轮次:2 / 20(16 领域全扫 + serde 语义镜头:三态/默认值/ExtraFields round-trip/数值时间戳/编码形态)
- 基线版本:同问题1(pinned OpenAPI `690521b`、openai-python `b19c2161`、openai-node `eea2292a`、Codex 0.144.5)
- 第 1 轮修复回归抽查:16 域全部通过(发现 2 处回归/不完整:2-A2、2-L1,均已列修)
- 本轮同时闭环 3 个遗留待定项:1-D1(拆分方案)、1-D2(采纳案三维持现状+决策)、1-D3(保持 10s+决策)

## 汇总表

| 编号 | 严重度 | 领域 | 问题 | 状态 |
|---|---|---|---|---|
| 2-01 | 中高 | L | `$ref` 兄弟键内联无循环终止,递归类型触发栈溢出 abort(D0129 回归) | 已修复 |
| 2-02 | 中 | C | SSE 默认单行 1 MiB 上限可拒绝官方大事件(partial_image 等) | 已修复 |
| 2-03 | 中 | I | VectorStoreMetadata 解码期硬校验 16/64/512,与 D0015/D0017 opt-in 策略矛盾 | 已修复 |
| 2-04 | 中低 | B/F | `OutputText.annotations`/`logprobs`(含 beta 侧、delta/done 事件)空数组再序列化丢 pin 必填键 | 已修复 |
| 2-05 | 中低 | H | `Batch.metadata` 解码期校验 16/64/512(同类策略矛盾) | 已修复 |
| 2-06 | 中低 | K | costs 查询 `bucket_width` 域过宽(应单值 1d)、`group_by` 未收紧 | 已修复 |
| 2-07 | 中低 | K | `AdminDataRetention.update_project` 缺方法,D0128 拆出的请求体未接线 | 已修复 |
| 2-08 | 中低 | P | RMCP `CompactWhenPossible` 展平静默丢弃富内容块,与文档承诺不符 | 已修复 |
| 2-09 | 中低 | P | `RealtimeConnectTarget` 未透传门面(call_id 连接唯一路径不可达) | 已修复 |
| 2-10 | 低 | B | compact 输出 StoredMessage role 收窄 3 值,critic/tool 等角色解码失败 | 已修复 |
| 2-11 | 低 | A | `HostedToolType` 发送域含 tool_choice 不存在的 `web_search`/`web_search_2025_08_26` | 已修复 |
| 2-12 | 低 | A | `CodeInterpreterNetworkAllowlist.validate()` 缺 `allowed_domains` minItems=1(D0117 不完整) | 已修复 |
| 2-13 | 低 | D/M | 查询参数显式 null/空串编码不一致:chat 报错、fine-tuning 发 `metadata=`、python 丢弃 | 已修复 |
| 2-14 | 低 | E | 每个入站 Ping 产生两个 Pong(tungstenite 自动+显式) | 已修复 |
| 2-15 | 低 | E | `RealtimePcmAudioFormat.rate` 裸 i64 可发 16000(pin 仅 24000) | 已修复 |
| 2-16 | 低 | F | beta compact `service_tier` 复用 7 值超集(pin 5 值) | 已修复 |
| 2-17 | 低 | F | multi-agent `call_id` 1..=64 校验缺失 | 已修复 |
| 2-18 | 低 | F | legacy `best_of > n` 关系校验缺失 | 已修复 |
| 2-19 | 低 | H | 文件/批量列表 limit 上限为发明值(10000/100),拒绝 pin 合法取值 | 已修复 |
| 2-20 | 低 | H | `BatchLine` 输入行 `deny_unknown_fields`,与输出行 ExtraFields 策略不一致 | 已修复 |
| 2-21 | 低 | H | `is_create_file_purpose`/`is_create_upload_purpose` 恒等且无调用点 | 已修复 |
| 2-22 | 低 | I | 文件批量 anyOf 互斥:对 schema 合法输入(files+全局字段)硬拒且错误信息误导 | 已修复 |
| 2-23 | 低 | J | `EvalSamplingParams` 三宿主并集,可发 pin 外字段与 null | 已修复 |
| 2-24 | 低 | J | alpha `Grader` 联合携带 Eval 资源侧 `pass_threshold`(pin 外) | 已修复 |
| 2-25 | 低 | K | `UpdateProjectServiceAccountBody.role` 含资源侧 `none` | 已修复 |
| 2-26 | 低 | L | 根自引用 `$ref:"#"` 误报 ExternalReference(实为根递归不可表示) | 已修复 |
| 2-27 | 低 | L | 唯一键非字符串 `$ref`(如 42)静默放行 | 已修复 |
| 2-28 | 低 | M | multipart `retry-after-ms` 超限/非有限时回落 `Retry-After`,与 transport 短路语义不一致 | 已修复 |
| 2-29 | 低 | M | `has_more=true`+`last_id=null` 流式分页硬失败,python 回退 `data[-1].id`;getter 与流口径不一 | 已修复 |
| 2-30 | 低 | N | `with_tolerance` 接受亚秒 Duration 但 `as_secs()` 截断为零窗口 | 已修复 |
| 2-31 | 低 | N | realtime.rs 重复 webhook 类型 `extra` 私有无访问器,与 webhooks.rs API 不对齐 | 已修复 |
| 2-32 | 低 | O | `text_elements` snake_case wire key 无测试固化 | 已修复 |
| 2-33 | 低 | O | `LoginAccountResponse.authUrl/verificationUrl` 用 `Url` 严于 pin string | 已修复 |
| 2-34 | 低 | P | `AdminCheckpointPermissions` 未透传门面 | 已修复 |
| 2-35 | 低 | P | `parse_function_arguments` 拒绝空串,零参工具不可调用 | 已修复 |
| 2-36 | 中低 | F | 1-D1 闭环:beta agent 三类 item 拆分 Param/Resource 双形态(id 必填+getter) | 已修复 |
| 2-37 | 低 | P/L | 清理 86 处裸 `"gpt-5.6"` 与 2 处 `"gpt-4.5"`(examples/README/client fixture/types fixture) | 已修复 |
| 2-38 | — | 多域 | 决策记录类:1-D2 关闭(案三)、1-D3 关闭(保持10s)、created_at i64、redirect 禁用、传输错误重试面、output_schema strict | 已修复(决策条目) |
| 2-R1 | — | C | 非流式 retrieve 不暴露 include_obfuscation/starting_after | 已拒绝(语义为空的合理收窄) |
| 2-R2 | — | F | `personality` 裸 String | 已拒绝(开放 string 设计) |
| 2-R3 | — | L | `NullableRepr` untagged 错误信息泄露私有类型名 | 已拒绝(信息级,serde 机制限制) |
| 2-D1 | — | E | Realtime WS 无 keepalive ping | 待定(行为取舍,后续评估配置项) |
| 2-D2 | — | F | `BetaPromptCachedInputMessage` 的 id/phase/status_null 是否移除 | 待定(D0050 依据需重读,下轮定夺) |

## 关键问题详情

### 2-01 `$ref` 兄弟键内联无循环终止【中高,D0129 回归,已实证】
- 位置:`crates/openai-rs-types/src/structured.rs:504-531`(内联)+ 429-439(base 快照)
- 证据:schemars 1.2.2 对带 doc 注释的递归类型(`Root{tree:Tree}`、`Tree{child:Box<Tree>}`)生成 `{$ref:"#/$defs/Tree",description}`;内联从不可变快照解析,每次展开重新引入同一 `$ref`+兄弟键→无界递归,实测 `fatal runtime error: stack overflow, aborting`(不可 catch)。公共入口 `StructuredOutput::<T>::new`/`TypedFunction::<A,R>::new` 直接可达。
- 修复:内联维护已解析指针链/深度预算,检测环返回错误(新增 `RecursiveReference` 或复用 `UnresolvableRef`,enum 已 non_exhaustive);文档声明根递归类型不支持;补带 doc 注释递归类型回归测试。与 2-26(`$ref:"#"` 错误归类)、2-27(非字符串 `$ref` 拒绝)一并处理。

### 2-02 SSE 单行 1 MiB 默认上限【中】
- 位置:`crates/openai-rs-client/src/sse.rs:25`、`431-435`
- 证据:python `_streaming.py:323-324` 与 node `streaming.ts:459-461` 均明言 "without imposing a line or event size limit";`response.image_generation_call.partial_image` 的 base64 载荷可达数 MiB。
- 修复:`DEFAULT_MAX_SSE_LINE_BYTES` 提至与 `DEFAULT_MAX_SSE_EVENT_BYTES` 同量级(32 MiB);补 >1MiB 单行事件回归测试。

### 2-04 必填数组空值 round-trip 丢键【中低】
- 位置:`responses.rs:3951-3954`(OutputText.annotations/logprobs)、`7951-7954`/`8005-8008`(delta/done 事件 logprobs)、`beta_responses.rs:1219-1222`
- 证据:pin `OutputTextContent.required` 含 annotations/logprobs(服务端恒发 `[]`);`skip_serializing_if="Vec::is_empty"` 使解码→再编码丢必填键;`to_input_items()` 回放路径实际可达。
- 修复:保留 `#[serde(default)]` 缺键容忍,移除 skip;补空数组 round-trip 与回放断言(beta 侧与事件侧同步)。

### 2-36 1-D1 闭环:beta agent item 拆分【中低】
- 方案(F 域代理评估,采纳):新增 `BetaAgentMessageParam`/`BetaMultiAgentCallParam`/`BetaMultiAgentCallOutputParam`(必填 author/recipient/content、call_id 1..=64 含 2-17、action、arguments/output;可选 id/agent);资源形 `id: String` 必填+getter,移除资源侧 null setter;`From<Resource> for Param`;联合变体替换;改动面经实测为零外部引用、全在 beta feature 内。compact output 与 input_items 列表维持输入联合宽松桥接(D0113 口径,决策记录)。

### 2-13/2-38 决策记录类
- 1-D2 关闭:multipart 空串照发、null 丢弃(D0126)维持——两官方 SDK 在空串上分裂(python 丢 `_qs.py` 空串短路、node 照发),null 上唯一非拒绝选项是丢弃;"不静默改写用户数据"为决胜规则;更正"空 prompt 非法"表述(仅 JSON image-edit prompt 有 minLength:1)。
- 1-D3 关闭:connect_timeout 保持 HTTP 10s/WS 30s 并文档化(node 实际生效 10s,python 5s,reqwest 默认 None;10s 处两基线之间)。
- created_at 保持 i64 + 决策(全 pin 120 处 integer、仅 Response/BetaResponse 2 处 number,python float;现实服务发整数秒)。
- redirect 禁用(`Policy::none()`)与官方 follow 相反——补决策条目(凭据不跨源原则,docs/architecture.md 已有原则句)。
- 传输错误重试面窄于 python(仅 connect/timeout)——补决策条目。
- `TypedFunction` output_schema 套用 strict 归一化+RootMustBeObject——补决策依据说明。

## 其余问题详情(摘要)

见各域代理报告要点,均已并入上表;修复分组派发时逐项携带完整证据(位置/证据/建议/置信度):
- 2-03/2-05:metadata 解码期校验移至 opt-in `validate()`(对齐 D0015/D0017 先例,Responses/FT 域已是裸 BTreeMap)。
- 2-06:`UsageCostsQueryParams` 拆分(bucket_width 单值 1d 开枚举、group_by 3 值开枚举)。2-25:更新体 role 2 值开枚举。
- 2-07:`AdminDataRetention::update_project(project_id, UpdateProjectDataRetentionBody) -> ApiResponse<ProjectDataRetention>`。
- 2-08:structuredContent 分支仅当 content 为空或全 text 块才展平,否则 lossless envelope;修文档;补测试。
- 2-10:StoredMessage 解码 role 放宽为开放 `MessageRole`(请求构造类型保留);补 critic/tool fixture(beta 同)。
- 2-11:`HostedToolType` 收窄为 pin tool-choice 8 值。
- 2-12:validate 补 `allowed_domains.is_empty()` 拒绝(对齐 containers 侧 EmptyAllowedDomains)。
- 2-14:Ping 分支 no-op(tungstenite 自动应答);补单 Ping 单 Pong 测试。
- 2-15:rate 类型化/校验 24000。
- 2-16:compact 5 值开枚举;2-17:call_id validate;2-18:best_of>n validate。
- 2-19:limit 仅保留下限 ≥1(files)——batches 上限 100 一并放宽或决策记录(python 无界)。
- 2-20:BatchLine 加 flatten ExtraFields;2-21:删两个恒等谓词。
- 2-22:`GlobalFieldsWithPerFileBatch` 独立错误变体;解码放宽 files+全局字段(schema 合法),builder 保持拒绝。
- 2-23:按宿主拆分三个 sampling params;2-24:alpha Grader 用无 pass_threshold 变体。
- 2-28:multipart `retry_delay` ms 分支解析成功即短路(对齐 transport/python/node);补双头并存测试。
- 2-29:`next_cursor` 在 data 非空时回退 `data.last()` id(python 语义)后再 fail-closed;统一 `next_after` 口径。
- 2-30:`with_tolerance` 拒绝 `<1s`;2-31:补 `extra_fields()`/`id()` 访问器。
- 2-32:补 `text_elements` wire-key round-trip 测试;2-33:DTO 放宽 String、公开类型构造处解析。
- 2-34:门面补 re-export;2-35:空串(纯空白)→ 空 JsonObject。
- 2-37:examples(5)/README(1)/client mock(2)改 pin 内模型;types 测试 fixture 81 处机械替换 `"gpt-5.6"`→`"gpt-5.6-sol"`。

## 结论

serde 镜头下共 38 项:1 中高(实证栈溢出)、2 中、6 中低、21 低、8 决策/清理类;3 项已拒绝、2 项新待定。第 1 轮修复回归 16/16 通过(2 处不完整已列修)。

## 修复记录(2026-08-31 执行完毕)

- 38/38 项闭环:2-01~2-37 代码修复(新增约 60 个测试函数),2-38 以决策条目 D0163 落档;1-D1 经 D0142 拆分闭环,1-D2/1-D3 经 D0163 关闭。决策条目 D0136–D0164(29 条)已追加 `spec/contracts/decisions.md`。
- 修复分组:组1 responses/beta(2-04/10/11/12/16/17/36)、组2 structured(2-01/26/27)、组3 client 传输(2-02/13/28/29)、组4 realtime(2-14/15/31)、组5 admin(2-06/07/25)、组6 files/batches/VS(2-03/05/19/20/21/22)、组7 evals/legacy(2-18/23/24)、组8 codex(2-32/33)、组9 rmcp/门面/webhooks(2-08/09/30/34/35)。
- 审计修正三处:2-31(访问器已由既有 `impl_extra_fields!` 宏提供,仅补测试)、2-14(双 Pong 在 tungstenite 0.29 wire 层不可复现,`set_additional` 对 pending Pong 是替换;仍移除冗余显式分支并同步 `responses_websocket.rs` 同型分支)、2-37 的 client 侧 `"gpt-4.5"`×2(经 pin 核实不在任何模型枚举,且仅为本地 mock 回环 fixture,非 wire 声称——**已拒绝**,维持原值)。
- 清理:types 测试 fixture 81 处 + examples 5 处 + README 1 处裸 `"gpt-5.6"` 机械替换为 pin 内 `"gpt-5.6-sol"`。
- 全部门禁通过:fmt、clippy(-D warnings 零错)、全量测试 **739 passed / 0 failed**、no-default-features、xtask check、cargo deny。
- 遗留:2-D1(realtime keepalive)、2-D2(BetaPromptCachedInputMessage 超集 setter)带入下轮;tagged-union 页面的分页回退待 types crate 统一 id 访问器(D0147 注明)。
- 审查整改:第2轮审查通过;D0153/D0154 决策文案已按审查意见修正。新增下轮待定项 **2-D3**:admin 域自带查询编码器(`client/admin.rs` `append_query_value`)对显式 null/空串仍发 `key=`,与 D0145 的 transport 规则不同源——下轮统一或补决策。
