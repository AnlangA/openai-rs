# 问题14.md — 第 14 轮复扫(R4 镜头:流式/错误通道第二遍)

- 日期:2026-09-01
- 轮次:14 / 20(R4 流式/错误镜头;16 域全量,3 域限流重派后完成)
- 基线:同前(pin OpenAPI、openai-python、openai-node、codex 0.144.5、rmcp 3.1.4、WHATWG SSE 规范)
- 总体:SSE 帧协议边缘矩阵、SSE 核心解码器、Admin 通道、misc 资源域零发现(L 域七组边缘逐一对齐规范+双 SDK 并实证运行 python 参考实现);发现集中在**官方 SDK 已提供的流式便利面缺失**、**同族谓词未同步**(D0264 未传导)、与**少量 EOF/关闭细节**

## 汇总表

| 编号 | 严重度 | 领域 | 问题 | 状态 |
|---|---|---|---|---|
| 14-C-1 | 中 | C | chat 流无累加器:python `ChatCompletionStreamManager`/`lib/streaming/chat`、node `ChatCompletionStream` 均提供 tool_calls 按 index 折叠+终态快照;我们 Responses 有 `ResponseAccumulator` 而 chat 面完全没有(内部不对称+超集缺失) | 已修复(D0270)|
| 14-E-1 | 中 | E | realtime rustdoc 与 D0199 出处断言"python 不重连 WS"已过期:pin 的 python 已带 opt-in 重连(`on_reconnecting`/5 次/1001,1005,1006,1011-1013,1015/退避抖动),node 仍不重连——文档失准 | 已修复(D0273,文档)|
| 14-F-1 | 中 | F | beta WS `response.create` 序列化时 `remove("stream_options")`,且 WS 发送入口无该字段——pin 的 WS create 模式含 `stream_options`(include_obfuscation),python/node 均透传;WS 通道无法表达混淆开关 | 已修复(D0274)|
| 14-F-2 | 中 | F | beta WS 握手无 extra-header 逃生口:官方两 SDK 的多代理示例都手动附 `OpenAI-Beta: responses_multi_agent=v1`,我们的固定头集合无法复现官方示例握手(D0210 只裁定不自动发送) | 已修复(D0274)|
| 14-I-1 | 中 | I | 共享轮询器忽略服务端 `openai-poll-after-ms` 步调提示(python `_vector_stores.py:16-22` 读取之,node `polling.ts:117-145` 同);VS 轮询无 preset 且默认 10 分钟 deadline 是两基线均不设的截断 | 已修复(D0275)|
| 14-A-1 | 低 | A | `ResponseStreamOptions` 缺 ExtraFields(同 body 唯一没有前向保留的嵌套选项结构;stream_options 重放有损) | 已修复(D0274)|
| 14-B-1 | 低 | B | EOF flush 派发 WHATWG 要求丢弃的未终止事件(双 SDK 丢弃,我们交付)——行为有意偏向交付,需落档 | 已落档(D0271) |
| 14-C-2 | 低 | C | chat 与 legacy completions 的 `event: error` 远端错误车道零测试覆盖(每通道两处手写分支无回归保护) | 已修复(D0270)|
| 14-C-3 | 低 | C | `classify()` 先查 remote-error 再查 `[DONE]`;python(_streaming.py:64)与 node(streaming.ts:106)均哨兵优先——同帧 `event:error`+`data:[DONE]` 时我们误报错误 | 已修复(D0271)|
| 14-D-1 | 低 | D | `Error::StreamProtocol` Display 硬编码 "Responses" 但 media 车道也构造它(频道误归因;D0195 同族) | 已修复(D0272)|
| 14-D-2 | 低 | D | D0264 的 `>=500` 未传导三处:`ApiError::is_server_error()`(error.rs:281)、WS InitialConnect 握手重试(responses_websocket.rs:630,三 WS 客户端共用)、`X509Error::is_retryable`(x509.rs:939) | 已修复(D0272)|
| 14-E-2 | 低 | E | 客户端主动 close 发空关闭帧(无 1000/reason);python `close(code=1000)`、node 两传输默认 1000/'OK';GA/beta WS 同病 | 已修复(D0273)|
| 14-E-3 | 低 | E | realtime 无 per-connection extra-header 逃生口与 WS base URL 覆盖(python `extra_headers`/`websocket_base_url`)——文档立场化即可 | 已落档(D0273) |
| 14-E-4 | 低 | E | WS send/recv 均 `&mut self`、无 split/无拥有任务模式文档(实时双向场景的必然需求) | 已修复(D0273,文档)|
| 14-F-3 | 低 | F | `BetaWebSocketErrorEvent.agent` 已解码但无访问器(多代理错误路由是核心用例;D0192 漏) | 已修复(D0274)|
| 14-F-4 | 低 | F | `BetaResponsesServerEvent::is_error()` 漏 `response.inject.failed`(测试只钉了 inject.created→false) | 已修复(D0274)|
| 14-F-5 | 低 | F | beta 面便利性缺口:无 `recv_into`/`event()` 访问器/`collect_with`(GA 面均有;`core()` 已暴露 GA 事件,机械补齐) | 已修复(D0274)|
| 14-F-6 | 低 | F | beta 流式方法缺 D0244 的"握手后不重试"注释(GA 面有) | 已修复(D0274)|
| 14-G-1 | 低 | G | `finish()` 在 RequireTerminal EOF 时丢弃已分类的 flush 帧:仅 data 无 `event:` 行的终态/错误帧被 `UnexpectedEof` 掩蔽(需两个服务端偏差同时发生) | 已修复(D0271)|
| 14-H-1 | 低 | H | 分页 fail-closed(无游标却 has_more / 游标回环)报 `InvalidConfiguration`("客户端配置无效"误导排障)+无 request_id(D0204 类别卫生族) | 已修复(D0276)|
| 14-H-2 | 低 | H | multipart 源预检吞掉 io::Error 细节(NotFound/PermissionDenied 折叠为同一消息,无 source 链) | 已修复(D0276)|
| 14-M-1 | 低 | M | 流式路径超时归属失准:非流式对标 node 成立,流式上我们是三者最严(python=per-read 600s,node=无),600s 截断为 `ResponseBody` 且无 deadline WARN——修文档+D0199 修订 | 已修复(D0276,文档)|
| 14-M-2 | 低 | M | 无流丢弃→连接关闭的端到端防泄漏测试(Drop 传播正确但无钉子,重构包 Arc/spawn 即漏) | 已修复(测试)|
| 14-O-1 | 低 | O | codex pin 合法的 `"result": null` JSON-RPC 响应解 typed 失败——`turn/interrupt` 被服务端接受却报 ResponseDecode(D0034/D0247 genre) | 已修复(D0277)|
| 14-P-1 | 低 | P | rmcp 客户端响应缓存可在 list_tools 失败时以陈旧首页掩蔽错误(SEP-2549 serve-stale 默认开)——需文档披露+失效开关指引 | 已修复(D0277,文档)|
| 14-P-2 | 低 | P | 每次 tools/call 宣告 progressToken 但不消费、固定截止不因进度延长(rmcp 提供 `reset_timeout_on_progress` 未用)——落档为立场 | 已落档(D0277) |
| 14-信息组 | 信息 | 多域 | B-2..6([DONE] 精确/无 error-key 车道/优先级注释/控制块/重试字段)、A-2/A-3(终态机制对比/类型态收紧)、C-4、D-3/D-4、E-信息、G-2(tts-1 系列不支持的 prose 未强制)、I-2/3/4(未知状态轮询立场/批 file_counts 基准/重试耗预算)、J-1/2/3(零发现)、K(零发现)、L(零发现+Responses 车道 error_is_truthy 观察)、M-3(span 枚举)、N-1、O-2/3、P-3/4 | 已落档(D0278) |

## 零发现域

- **L/SSE 核心**:七组边缘(colon/空格、event 空白、字段名大小写、限额、UTF-8 分片、id 语义、无名事件)与规范+双 SDK 逐字节一致(实证运行 python 参考解码器)
- **K/Admin**:122 op 全走单一共享重试/错误路径,D0264/D0265 经共享代码自动覆盖;无 idempotency 差异、无超时差异
- **J/misc**:evals 无官方流式面(poll-only 即对齐),Domain J 零 SSE 路径,错误通道零旁路

## 修复分工(按文件所有权,6 组)

- 组1(client sse/response_stream/media/error):14-C-3、14-G-1、14-D-1、14-D-2a、14-M-2
- 组2(client responses_websocket/realtime/x509):14-D-2b/c、14-E-1、14-E-2、14-E-3、14-E-4
- 组3(client chat_stream/completions + 新累加器):14-C-1、14-C-2
- 组4(types responses + types/client beta_responses):14-A-1、14-F-1..6
- 组5(client poll/pagination/multipart/vector_stores/transport 文档 + 分页调用方):14-I-1、14-H-1、14-H-2、14-M-1
- 组6(codex app_server/client + rmcp 文档):14-O-1、14-P-1、14-P-2
- 主代理:信息组落档(D0270+)

## 结论

R4 复扫:协议核心(SSE 解码、错误车道枚举、重试真值表)全面收敛且多处实证对齐;残余集中在官方流式便利面(chat 累加器、beta WS 表达力)、D0264 类谓词的三处未传导、以及 EOF/关闭/错误类别等收尾细节。

## 修复记录(2026-09-01)

6 个修复代理按文件所有权并行,全部完成并落盘;D0270-D0278 落档。要点:

| 组 | 内容 | 落档 |
|---|---|---|
| G1(sse/response_stream/media/error) | 哨兵优先于 remote-error;`finish_with_flushed()` EOF 帧交付(media/Responses 采纳);`StreamProtocol` 措辞中性;`is_server_error` 数值化;流丢弃→连接关闭端到端测试 | D0271/D0272/D0278 |
| G2(responses_websocket/realtime/x509) | WS 握手重试与 x509 `>=500`;close 发 1000(服务端观察者夹具钉线);realtime 重连文档重锚(python opt-in 车道如实记述)+类型化派生立场+拥有任务模式文档 | D0272/D0273 |
| G3(chat_accumulator 新模块+chat_stream/completions) | `ChatCompletionAccumulator` 全量折叠(tool_calls 按 index、usage 块、python 优先级规则)+`collect_with` 便利;两通道 `event: error` 回环测试 | D0270 |
| G4(types responses 局部+types/client beta) | WS create 保留 stream_options+可设置;`with_beta_header`/校验过的 `extra_static_header`(默认关,D0210 保持);`agent()` 访问器;is_error 钉子;`event()`/`collect_with` 便利;D0244 注释;`ResponseStreamOptions` ExtraFields | D0274 |
| G5(poll/operation/pagination/multipart/VS+调用方) | `openai-poll-after-ms` 保留+accessor;server-paced 轮询模式+`for_vector_stores()` preset;`Error::Pagination{resource,reason}` 取代 InvalidConfiguration(全调用方);multipart 源三态消息+io 细节内联;超时三方真相文档 | D0275/D0276 |
| G6(codex client+rmcp) | null result 归一化 `json!({})`(turn/interrupt 回归);rmcp 缓存新鲜度+进度立场文档 | D0277 |

验证:fmt/clippy 全绿(含最小特性组合),workspace 测试 1210 通过/0 失败(+51),feature/MSRV/xtask/deny 全绿;修复内容目标测试(chat_accumulator 全套、event:error 回环、poll pacing、pagination faults、EOF flush、close 1000、null result 等)全部包含在内。
